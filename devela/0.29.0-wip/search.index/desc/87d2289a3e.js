rd_("DcCasts from <code>isize</code> to <code>u16</code> with range check.DaCasts from <code>i64</code> to <code>u16</code> with range check.DbCasts from <code>i128</code> to <code>u16</code> with range check.D`Casts from <code>i8</code> to <code>u16</code> with range check.DbCasts from <code>u128</code> to <code>u16</code> with range check.D`Casts from <code>u8</code> to <code>u16</code> with range check.DaCasts from <code>u32</code> to <code>u16</code> with range check.CmCasts <code>self</code> to <code>u32</code> with range check.00DaCasts from <code>u16</code> to <code>u32</code> with range check.D`Casts from <code>u8</code> to <code>u32</code> with range check.DaCasts from <code>i16</code> to <code>u32</code> with range check.DcCasts from <code>isize</code> to <code>u32</code> with range check.DaCasts from <code>u32</code> to <code>u32</code> with range check.D`Casts from <code>i8</code> to <code>u32</code> with range check.DbCasts from <code>u128</code> to <code>u32</code> with range check.DbCasts from <code>i128</code> to <code>u32</code> with range check.DaCasts from <code>u64</code> to <code>u32</code> with range check.DaCasts from <code>i32</code> to <code>u32</code> with range check.DaCasts from <code>i64</code> to <code>u32</code> with range check.DcCasts from <code>usize</code> to <code>u32</code> with range check.CmCasts <code>self</code> to <code>u64</code> with range check.00D`Casts from <code>i8</code> to <code>u64</code> with range check.DbCasts from <code>i128</code> to <code>u64</code> with range check.DaCasts from <code>u64</code> to <code>u64</code> with range check.DbCasts from <code>u128</code> to <code>u64</code> with range check.DaCasts from <code>u16</code> to <code>u64</code> with range check.DaCasts from <code>u32</code> to <code>u64</code> with range check.DaCasts from <code>i16</code> to <code>u64</code> with range check.DaCasts from <code>i32</code> to <code>u64</code> with range check.DaCasts from <code>i64</code> to <code>u64</code> with range check.DcCasts from <code>usize</code> to <code>u64</code> with range check.DcCasts from <code>isize</code> to <code>u64</code> with range check.D`Casts from <code>u8</code> to <code>u64</code> with range check.CdLow lane <code>a == b</code>, other lanes unchanged.CkLow lane <code>a &amp;gt;= b</code>, other lanes unchanged.CjLow lane <code>a &amp;gt; b</code>, other lanes unchanged.CkLow lane <code>a &amp;lt;= b</code>, other lanes unchanged.CiLow lane <code>a &amp;lt; b</code>, other lane unchanged.BdLow lane <code>f64</code> less than.CdLow lane <code>a != b</code>, other lanes unchanged.CnLow lane <code>!(a &amp;gt;= b)</code>, other lanes unchanged.CmLow lane <code>!(a &amp;gt; b)</code>, other lanes unchanged.CnLow lane <code>!(a &amp;lt;= b)</code>, other lanes unchanged.CmLow lane <code>!(a &amp;lt; b)</code>, other lanes unchanged.DhCompare <code>f64</code> lanes according to the operation specified, \xe2\x80\xa6BnLanewise <code>a.is_nan() | b.is_nan()</code>.CkDecodes a bounded column-major ordinal into 2D coordinates.CkDecodes a bounded column-major ordinal into 3D coordinates.CeReturns the reserved capacity for elements in the \xe2\x80\xa60BbReturns <code>NotSupported</code>.0CmThe capacity of a fixed-size array is always equal to its \xe2\x80\xa6122EdReturns <code>true</code> if the collection contains the given <code>element</code>.02DeThis is less efficent than <code>HashSet::contains</code> for not \xe2\x80\xa6DfThis is less efficent than <code>BTreeSet::contains</code> for not \xe2\x80\xa622EfReturns <code>true</code> if the collection is empty, <code>false</code> if it\xe2\x80\x99s \xe2\x80\xa60DgReturns <code>NotSupported</code> since a fixed-size array is never \xe2\x80\xa611CbReturns the number of direct color channel fields.DmReturns <code>true</code> if all bits in <code>ALT_SCREEN</code> are present.DmReturns <code>true</code> if all bits in <code>BLINK_FAST</code> are present.CoReturns <code>true</code> if this set contains <code>ch</code>.DmReturns <code>true</code> if all bits in <code>COLOR_PAIR</code> are present.EbReturns <code>true</code> if the set contains all bits in <code>IDENT_HEAD</code>.EbReturns <code>true</code> if the set contains all bits in <code>IDENT_TAIL</code>.DmReturns <code>true</code> if all bits in <code>MOUSE_DRAG</code> are present.DmReturns <code>true</code> if all bits in <code>SA_NODEFER</code> are present.DmReturns <code>true</code> if all bits in <code>SA_ONSTACK</code> are present.DmReturns <code>true</code> if all bits in <code>SA_RESTART</code> are present.DmReturns <code>true</code> if all bits in <code>SA_SIGINFO</code> are present.FjCopies <code>count * size_of::&lt;T&gt;()</code> bytes from <code>src</code> to <code>dst</code>. Must \xe2\x80\xa6C`Create a new empty string with a provided bufferCnGets an <code>m128i</code> value out of an <code>m256i</code>.BcReturns the count of prime factors.00000000000CoFinds the index of the last 1 in <code>self</code> from the \xe2\x80\xa600000CmReturns the greatest UTF-8 boundary <code>&lt;= index</code>.CbA wrapper over <code>floor_utf8_boundary()</code>.CcCalls <code>f</code> for each ALSA PCM device hint.DhReturns the number of frames in <code>data_len</code>, if frame-aligned.0CmCreates a C string wrapper from a byte slice with exactly \xe2\x80\xa6CiCreates a new <code>MaybeNiche</code> without any checks.00000000000000000000000000000000000000000000000FbConstructs a <code>Layout</code> from a given <code>size</code> and <code>alignment</code>, or \xe2\x80\xa6EhCreates a new <code>MaybeNiche</code> from a <code>usize</code>, wrapping at numeric \xe2\x80\xa600000000000000000000000000000000000000000000000DhConverts a vector of bytes to a <code>String</code> without checking \xe2\x80\xa6CgConverts a slice of bytes to a string slice without \xe2\x80\xa6DjCreates a <code>charu</code> from the first UTF-8 encoded scalar value \xe2\x80\xa6DjCreates a <code>charu_niche</code> from the first UTF-8 encoded scalar \xe2\x80\xa6BgLanewise fused <code>(a * b) + c</code>0BgLanewise fused <code>(a * b) - c</code>0AhReturns the field value.CjGets the low lane as an individual <code>f32</code> value.BnReturns the glyph for the specified character.DkConverts the low lane to <code>i32</code> and extracts as an individual \xe2\x80\xa6CkCalculates the 1D array index from the given 2D coordinates0CkGets the mutable reference of the contents of the cell, \xe2\x80\xa60DeReturns <code>true</code> if <code>BRACKETED_PASTE</code> is present.CeReturns whether the byte stream contains complete \xe2\x80\xa6CiReturns whether the sample data contains complete frames.CcReturns whether the planes contain complete frames.ChReturns whether the plane data contains complete frames.2BhChecks if mutable-globals are available.ChReturns whether this format has a padding channel or \xe2\x80\xa6EaInserts a lane from <code>$b</code> into <code>$a</code>, optionally at a new \xe2\x80\xa6CaInserts an <code>i16</code> to <code>m256i</code>CaInserts an <code>i32</code> to <code>m256i</code>CaInserts an <code>i64</code> to <code>m256i</code>ClInserts a new value for the <code>i64</code> lane specified.CaInserts an <code>m128</code> to <code>m256</code>AeInserts output flags.BcCalls `NumInt::int_ref_combine_rep.CjSimilar to int_combine_rep, but operates on references \xe2\x80\xa6101100BcCalls `NumInt::int_ref_digits_base.CjSimilar to int_digits_base, but operates on references \xe2\x80\xa6101100BcCalls `NumInt::int_ref_digits_sign.CjSimilar to int_digits_sign, but operates on references \xe2\x80\xa6101100BcCalls `NumInt::int_ref_factors_buf.CjSimilar to int_factors_buf, but operates on references \xe2\x80\xa6101100BcCalls `NumInt::int_ref_permute_rep.CjSimilar to int_permute_rep, but operates on references \xe2\x80\xa6101100DoReturns <code>true</code> if the set shares any bit with <code>EXTENDED</code>.DoReturns <code>true</code> if the set shares any bit with <code>EXTERNAL</code>.DoReturns <code>true</code> if the set shares any bit with <code>KEYBOARD</code>.DoReturns <code>true</code> if the set shares any bit with <code>NUM_LOCK</code>.DoReturns <code>true</code> if the set shares any bit with <code>OVERLINE</code>.DoReturns <code>true</code> if the set shares any bit with <code>Shutdown</code>.DoReturns <code>true</code> if the set shares any bit with <code>SIGWINCH</code>.DoReturns <code>true</code> if the set shares any bit with <code>Variant0</code>.DoReturns <code>true</code> if the set shares any bit with <code>Variant1</code>.DoReturns <code>true</code> if the set shares any bit with <code>Variant2</code>.DoReturns <code>true</code> if the set shares any bit with <code>Variant3</code>.DoReturns <code>true</code> if the set shares any bit with <code>Variant4</code>.DdEquivalent to <code>CString::into_bytes()</code> except that the \xe2\x80\xa6CnReturns a tuple with the active variant\xe2\x80\x99s inner value in \xe2\x80\xa6DfReturns <code>true</code> if operations on values of this type are \xe2\x80\xa6000000000000000ClReturns whether the raw signal number can be caught by a \xe2\x80\xa6CeReturns <code>true</code> if the field value is zero.CeOptional faster combining mark check for common text.0DgReturns <code>true</code> for common combining marks used in modern \xe2\x80\xa6011C`Optional faster fullwidth check for common text.0DdReturns <code>true</code> for common fullwidth characters (ASCII \xe2\x80\xa601144D`Returns <code>true</code> if the field value equals its maximum.DdWhether <code>self</code> is equal to <code>NUM_MAX_NEGATIVE</code>.000FdWhether <code>self</code> is equal to <code>Some(</code><code>NUM_MIN_POSITIVE</code><code>)</code>.000CfTrue if the pointer crossed into or out of the window.88DaReturns <code>self</code> with only the most significant bit set.00000000000CoIterator over generated IDs with <code>Relaxed</code> ordering.BdThe stream layout is not configured.DbLoads the <code>f32</code> reference into all lanes of a register.DkLoad an <code>f32</code> and splat it to all lanes of an <code>m256d</code>BdLoads the reference into a register.BfLoad data from memory into a register.CeAttempts to grow the default linear memory by the \xe2\x80\xa6DjReturns <code>self</code> with full mouse motion reporting, preferring \xe2\x80\xa6EfReturns the middle sub-<code>string</code> with the given maximum <code>len</code> \xe2\x80\xa6ClConstructs a new boxed slice with uninitialized contents \xe2\x80\xa6CaConstructs a new reference-counted slice with \xe2\x80\xa610CmReturns the next Unicode scalar, without performing UTF-8 \xe2\x80\xa6EdReturns an iterator over <code>charu</code> scalars in <code>s</code> with their \xe2\x80\xa6CcReturns the next complete grapheme cluster as a \xe2\x80\xa6DfNormalize a path, including <code>..</code> without traversing the \xe2\x80\xa6BfCreates an insufficient-element error.CiDuplicates the next of stack pair of elements to the top.CjSwaps the top two pair stack elements in compile-time, \xe2\x80\xa6CjDuplicates the top pair of elements before the next of \xe2\x80\xa6CfRemoves and returns a copied value from the front, \xe2\x80\xa6DjClones the elements at the range <code>src</code> and prepends them to \xe2\x80\xa6BlUnchecked version of <code>push_back</code>.DcPushes a new <code>element</code> to the front of the destaque, \xe2\x80\xa6CaAttempts to fill the buffer with generated bytes.000DnReturns a subslice from <code>start</code> (inclusive) to <code>end</code> \xe2\x80\xa6DoReturns a substring from <code>start</code> (inclusive) to <code>end</code> \xe2\x80\xa6EhReturns an exclusive subslice from <code>start</code> (inclusive) to <code>end</code>\xe2\x80\xa6CgReturns the logical raster extent in samples or pixels.00EdReads ASCII bytes from <code>bytes</code> into <code>out</code>, stopping at the \xe2\x80\xa6AeRemoves output flags.ChDecodes a bounded row-major ordinal into 2D coordinates.ChDecodes a bounded row-major ordinal into 3D coordinates.BbSets <code>BRACKETED_PASTE</code>.CiReplaces the field value, masking it to fit the field \xe2\x80\xa6CbEnables or disables interrupt generation on BREAK.DaSets <code>ENTROPY_WEAK</code> if <code>condition</code> is true.DaSets <code>FOCUS_EVENTS</code> if <code>condition</code> is true.DaSets <code>MOUSE_MOTION</code> if <code>condition</code> is true.DaSets <code>PROPORTIONAL</code> if <code>condition</code> is true.DaSets <code>REPRODUCIBLE</code> if <code>condition</code> is true.DaSets <code>SA_NOCLDSTOP</code> if <code>condition</code> is true.DaSets <code>SA_NOCLDWAIT</code> if <code>condition</code> is true.DaSets <code>SA_RESETHAND</code> if <code>condition</code> is true.DcSplats the <code>i16</code> to all lanes of the <code>m128i</code>.CnSplat an <code>i16</code> arg into an <code>m256i</code> lane.DcSplats the <code>i32</code> to all lanes of the <code>m128i</code>.CnSplat an <code>i32</code> arg into an <code>m256i</code> lane.DdSplats the <code>i64</code> to both lanes of the <code>m128i</code>.CnSplat an <code>i64</code> arg into an <code>m256i</code> lane.DjShifts the vector elements to the left by <code>OFFSET</code>, filling \xe2\x80\xa6EmApplies the sign of <code>i8</code> values in <code>b</code> to the values in <code>a</code>.DaLanewise <code>a * signum(b)</code> with lanes as <code>i8</code>CdDetermines if the <code>0</code> flag was specified.CnTakes a substring based on a range that corresponds to the \xe2\x80\xa6DcReturns the string slice covered by <code>range</code>, without \xe2\x80\xa6DiSplat the lower <code>f64</code> across both lanes of <code>m128d</code>.CgReturns stdout as a UTF-8 string, replacing invalid \xe2\x80\xa600ChDrives one runtime iteration including rendering and \xe2\x80\xa6DgSubtract each lane horizontally, pack the outputs as <code>a</code> \xe2\x80\xa6BiSubtract adjacent <code>f32</code> lanes.BlUnchecked version of <code>swap_back</code>.EhSwaps the two regions of memory beginning at <code>x</code> and <code>y</code>. Must \xe2\x80\xa6DjRemoves and returns the value at logical <code>index</code>, replacing \xe2\x80\xa6CgPrimitive-index variant of <code>take_copy_with</code>.CfReturns the last <code>n</code> elements of the slice.CkConsumes a single-quoted literal string and returns the \xe2\x80\xa6AhTests if all bits are 1.BnConverts <code>elapsed</code> to microseconds.0000000BnConverts <code>elapsed</code> to milliseconds.0000000DbReturns the current timestamp as milliseconds in <code>f64</code>.0000000CbA wrapper over <code>to_scalar_unchecked()</code>.CnDecodes a UTF-8 scalar from the given byte slice, starting \xe2\x80\xa6DkConverts the value into a <code>usize</code>, saturating at the numeric \xe2\x80\xa600000000000000000000000000000000000000000000000CnTranspose four <code>m128</code> as if they were a 4x4 matrix.C`Moves the cursor to zero-based grid coordinates.CgReturns a time delta corresponding to the number of \xe2\x80\xa6BeReplaces the field value, if it fits.0000DjReturns <code>self</code> with the checked field value replaced, if it \xe2\x80\xa60E`Returns a new <code>LinuxTimespec</code> with the given <code>time_delta</code>.EeUnpack and interleave low <code>i8</code> lanes of <code>a</code> and <code>b</code>.0BbUnsets <code>CLEAR_ON_DROP</code>.BbUnsets <code>CRYPTOGRAPHIC</code>.BbUnsets <code>DETERMINISTIC</code>.CoUnsets the bits in <code>self</code> to 0, from the checked \xe2\x80\xa600000BlWebAssembly mouse event callback dispatcher.BlWebAssembly wheel event callback dispatcher.DdReturns <code>self</code> with <code>CLEAR_ON_ENTER</code> inserted.BlSets the <code>Content-Length</code> header.DdReturns <code>self</code> with <code>GENERATOR_WEAK</code> inserted.FaReturns <code>self</code> with <code>HIDE_CURSOR</code> inserted if <code>condition</code> is \xe2\x80\xa6FaReturns <code>self</code> with <code>SA_RESTORER</code> inserted if <code>condition</code> is \xe2\x80\xa6FaReturns <code>self</code> with <code>SCROLL_LOCK</code> inserted if <code>condition</code> is \xe2\x80\xa6FaReturns <code>self</code> with <code>SUPERSCRIPT</code> inserted if <code>condition</code> is \xe2\x80\xa6FaReturns <code>self</code> with <code>SYNC_UPDATE</code> inserted if <code>condition</code> is \xe2\x80\xa6FaReturns <code>self</code> with <code>TOUCHSCREEN</code> inserted if <code>condition</code> is \xe2\x80\xa6D`Returns <code>self</code> with <code>HIDE_CURSOR</code> removed.D`Returns <code>self</code> with <code>SA_RESTORER</code> removed.D`Returns <code>self</code> with <code>SCROLL_LOCK</code> removed.D`Returns <code>self</code> with <code>SUPERSCRIPT</code> removed.D`Returns <code>self</code> with <code>SYNC_UPDATE</code> removed.D`Returns <code>self</code> with <code>TOUCHSCREEN</code> removed.BiSends a message to a specific Web Worker.DjCasts <code>self</code> to <code>i8</code> wrapping at the numeric bounds.00DnCasts from <code>i64</code> to <code>i8</code> wrapping at the numeric bounds.DmCasts from <code>u8</code> to <code>i8</code> wrapping at the numeric bounds.DmCasts from <code>i8</code> to <code>i8</code> wrapping at the numeric bounds.DnCasts from <code>u32</code> to <code>i8</code> wrapping at the numeric bounds.DnCasts from <code>u64</code> to <code>i8</code> wrapping at the numeric bounds.E`Casts from <code>isize</code> to <code>i8</code> wrapping at the numeric bounds.E`Casts from <code>usize</code> to <code>i8</code> wrapping at the numeric bounds.DnCasts from <code>i32</code> to <code>i8</code> wrapping at the numeric bounds.DoCasts from <code>u128</code> to <code>i8</code> wrapping at the numeric bounds.DnCasts from <code>i16</code> to <code>i8</code> wrapping at the numeric bounds.DoCasts from <code>i128</code> to <code>i8</code> wrapping at the numeric bounds.DnCasts from <code>u16</code> to <code>i8</code> wrapping at the numeric bounds.DjCasts <code>self</code> to <code>u8</code> wrapping at the numeric bounds.00DoCasts from <code>u128</code> to <code>u8</code> wrapping at the numeric bounds.DnCasts from <code>i64</code> to <code>u8</code> wrapping at the numeric bounds.DnCasts from <code>u64</code> to <code>u8</code> wrapping at the numeric bounds.E`Casts from <code>isize</code> to <code>u8</code> wrapping at the numeric bounds.DnCasts from <code>u16</code> to <code>u8</code> wrapping at the numeric bounds.DmCasts from <code>u8</code> to <code>u8</code> wrapping at the numeric bounds.DmCasts from <code>i8</code> to <code>u8</code> wrapping at the numeric bounds.DoCasts from <code>i128</code> to <code>u8</code> wrapping at the numeric bounds.DnCasts from <code>i16</code> to <code>u8</code> wrapping at the numeric bounds.DnCasts from <code>i32</code> to <code>u8</code> wrapping at the numeric bounds.E`Casts from <code>usize</code> to <code>u8</code> wrapping at the numeric bounds.DnCasts from <code>u32</code> to <code>u8</code> wrapping at the numeric bounds.CfWrites 1..=39 decimal digits without leading zeros \xe2\x80\xa6CfWrites 1..=20 decimal digits without leading zeros \xe2\x80\xa6CnWrites 1..=3 decimal digits without leading zeros starting \xe2\x80\xa6CfWrites 1..=10 decimal digits without leading zeros \xe2\x80\xa6CnWrites 1..=5 decimal digits without leading zeros starting \xe2\x80\xa63CmWrites a length prefix into this hasher, as part of being \xe2\x80\xa6000BdCode to disable focus in/out events.BgCode to disable mouse motion reporting.BkWAVEFORMATEXTENSIBLE extension byte length.BhExtended pictographic code point (emoji)00BjA required X11 extension is not available.00Bf<code>507 Insufficient Storage</code>.Bi\xf0\x9f\x90\xa7 \xf0\x9f\xae\x96 A Linux termios character size.00BfDecreases the microphone input volume.00Cm\xf0\x9f\xaa\xaa \xf0\x9f\x9a\xa9 The dimensions given did not match the elements \xe2\x80\xa600C`A container chunk is missing its form/list type.00Ee\xf0\x9f\xa7\xb5 \xf0\x9f\x9a\xa9+ <code>std</code> Possible errors that made [<code>recv_timeout</code>][\xe2\x80\xa600CnThe maximum representable power of two within the type\xe2\x80\x99s \xe2\x80\xa600Eg\xf0\x9f\x93\x81 \xf0\x9f\x9a\xa9 <code>std</code> An error returned from [<code>Path::strip_prefix</code>] \xe2\x80\xa600CeQuery cursor visibility mode support/state. (DECTCEM)CeWAVEFORMATEXTENSIBLE IEEE float subformat GUID bytes.De\xf0\x9f\x95\x98 A time split from hours to seconds, normalized <em>(80b </em>\xe2\x80\xa600Db\xf0\x9f\x95\x98 A time split from years to days, normalized <em>(80b </em>\xe2\x80\xa600Df\xf0\x9f\x95\x98 A time split from years to seconds, normalized <em>(104b </em>\xe2\x80\xa600CeFound unexpected trailing data after a closing quote.00CdThe WAVE extensible subformat GUID is not supported.00EcAdd each lane horizontally, pack the outputs as <code>a</code> then <code>b</code>.BdAdd adjacent <code>f64</code> lanes.DoReturns the stored <code>f32</code> milliseconds truncated to <code>u32</code>.DmReturns a tuple with <code>Some(&amp;value)</code> for the active variant \xe2\x80\xa6Ch\xf0\x9f\x92\xaf Asserts the approximate equality of a series of \xe2\x80\xa600DhAsserts that the memory alignment of the <code>Candidate</code> type \xe2\x80\xa600DdBinary searches this <code>VecDeque</code> with a key extraction \xe2\x80\xa6EdCounts the number of 1s in <code>self</code> from the <code>[start..=end]</code> \xe2\x80\xa600CgReturns true if the <code>nth</code> bit is unset to 0.00B`Returns metadata for this field.CiReturns the byte distance between adjacent channel bases.FaAdds a <code>Duration</code> to this <code>TimeDelta</code>. Returns <code>None</code> if the \xe2\x80\xa6CnCasts <code>self</code> to <code>i128</code> with range check.00DbCasts from <code>u16</code> to <code>i128</code> with range check.DbCasts from <code>i32</code> to <code>i128</code> with range check.DcCasts from <code>u128</code> to <code>i128</code> with range check.DbCasts from <code>i64</code> to <code>i128</code> with range check.DbCasts from <code>u64</code> to <code>i128</code> with range check.DbCasts from <code>u32</code> to <code>i128</code> with range check.DcCasts from <code>i128</code> to <code>i128</code> with range check.DbCasts from <code>i16</code> to <code>i128</code> with range check.DaCasts from <code>i8</code> to <code>i128</code> with range check.DdCasts from <code>usize</code> to <code>i128</code> with range check.DdCasts from <code>isize</code> to <code>i128</code> with range check.DaCasts from <code>u8</code> to <code>i128</code> with range check.CnCasts <code>self</code> to <code>u128</code> with range check.00DbCasts from <code>u32</code> to <code>u128</code> with range check.DbCasts from <code>i64</code> to <code>u128</code> with range check.DdCasts from <code>isize</code> to <code>u128</code> with range check.DdCasts from <code>usize</code> to <code>u128</code> with range check.DaCasts from <code>i8</code> to <code>u128</code> with range check.DcCasts from <code>u128</code> to <code>u128</code> with range check.DcCasts from <code>i128</code> to <code>u128</code> with range check.DbCasts from <code>u16</code> to <code>u128</code> with range check.DbCasts from <code>i16</code> to <code>u128</code> with range check.DaCasts from <code>u8</code> to <code>u128</code> with range check.DbCasts from <code>i32</code> to <code>u128</code> with range check.DbCasts from <code>u64</code> to <code>u128</code> with range check.FdSubtracts a <code>Duration</code> from this <code>TimeDelta</code>. Returns <code>None</code> if \xe2\x80\xa6CkLanewise <code>a == b</code> with lanes as <code>i8</code>.ChCompare <code>i8</code> lanes for equality, mask output.DaLanewise <code>a &amp;gt; b</code> with lanes as <code>i8</code>.DiCompare <code>i8</code> lanes for <code>a &amp;gt; b</code>, mask output.DaLanewise <code>a &amp;lt; b</code> with lanes as <code>i8</code>.CcLow lane <code>a != b</code>, other lane unchanged.CmLow lane <code>!(a &amp;gt;= b)</code>, other lane unchanged.ClLow lane <code>!(a &amp;gt; b)</code>, other lane unchanged.CmLow lane <code>!(a &amp;lt;= b)</code>, other lane unchanged.ClLow lane <code>!(a &amp;lt; b)</code>, other lane unchanged.BnLanewise <code>a.is_nan() | b.is_nan()</code>.CaReturns a vector containing the color components.000DnReturns <code>true</code> if all bits in <code>HIDE_CURSOR</code> are present.DnReturns <code>true</code> if all bits in <code>SA_RESTORER</code> are present.DnReturns <code>true</code> if all bits in <code>SCROLL_LOCK</code> are present.DnReturns <code>true</code> if all bits in <code>SUPERSCRIPT</code> are present.DnReturns <code>true</code> if all bits in <code>SYNC_UPDATE</code> are present.DnReturns <code>true</code> if all bits in <code>TOUCHSCREEN</code> are present.BlReturns a copy of the next new random state.Dj\xf0\x9f\x92\xaf <code>core</code> Asserts that an expression matches the provided \xe2\x80\xa600ChReturns the required output buffer size for decoding \xe2\x80\xa6000000000ClDrops the top of stack element, replacing the underlying \xe2\x80\xa6CiChecks that two strings are an ASCII case-insensitive \xe2\x80\xa6CiCompares two arrays for equality, ignoring ASCII case \xe2\x80\xa6CfExtends the back of the destaque from an iterator, \xe2\x80\xa6BaReturns the unique prime factors.00000000000D`Finds the index of the first 1 in <code>self</code> from the \xe2\x80\xa600000CoFinds the index of the last 0 in <code>self</code> from the \xe2\x80\xa600000CgWrites formatted output into the given byte buffer, \xe2\x80\xa6BnALSA reported more frames than were requested.ChPrimitive-index variant of <code>from_array_ring</code>.0CmCreates a buffer from an array of options and an explicit \xe2\x80\xa6C`Creates a buffer from raw uninitialized storage.DkReturns a string from an array of <code>bytes</code> that must be valid \xe2\x80\xa60EfCreates a buffer from the first <code>len</code> bytes of <code>buf</code> without \xe2\x80\xa6CiCreates a C string wrapper from a byte slice with any \xe2\x80\xa6DaCreates a new <code>StringNonul</code> from a <code>charu</code>.DcCreates a new <code>GraphemeNonul</code> from a <code>charu</code>.D`Creates a new <code>GraphemeU8</code> from a <code>charu</code>.BoCreates a new string from a <code>charu</code>.DdConverts a <code>Duration</code>\xe2\x80\x99s sub-second component into a \xe2\x80\xa60EcConverts a timed normalized <code>EventKey</code> back to <code>WebEventKey</code>.DoReturns a <code>Month</code> from its index, from <code>January=0</code> to \xe2\x80\xa6CjCreates a PCM buffer from mutably borrowed interleaved \xe2\x80\xa6DgCreates a new event with the given <code>target and timed </code>kind`.Bg<em>const</em> method equivalent to \xe2\x80\xa600ClCreates a new buffer by moving all the possible elements \xe2\x80\xa6ChCreates a buffer over a shared slice, truncating the \xe2\x80\xa6ClCreates a buffer over an exclusive slice, truncating the \xe2\x80\xa6CjCreates a <code>charu</code> from an array of UTF-8 bytes.D`Creates a <code>charu_niche</code> from an array of UTF-8 bytes.CnLow lane fused <code>(a * b) + c</code>, other lanes unchangedCoLow lane fused <code>(a * b) - c</code>, other lanes unchanged.DgReturns the number of IDs generated so far with <code>SeqCst</code> \xe2\x80\xa6CkCalculates the 2D coordinates from the given 1D array index0CaGets the lower lane as an <code>f64</code> value.CeConverts the lower lane to an <code>i32</code> value.0CeConverts the lower lane to an <code>i64</code> value.0AhReturns the field value.0DfReturns <code>true</code> if <code>MOUSE_SGR_PIXELS</code> is present.DfReturns <code>true</code> if <code>PROCESSED_OUTPUT</code> is present.DfReturns <code>true</code> if <code>UNDERLINE_DOUBLE</code> is present.DeComputes a hash of a byte slice using a <strong>custom seed</strong>.00AnIdeographic baseline position.00AfInserts control flags.ClInserts a new value for the <code>i32</code> lane specified.ClInserts a new value for the <code>i64</code> lane specified.ClReturns the number of digits in the given <code>base</code>.000CfReturns the quotient, rounding ties towards from zero.000BdCalls `NumInt::int_ref_digital_root.CkSimilar to int_digital_root, but operates on references \xe2\x80\xa6101100BdCalls `NumInt::int_ref_div_ties_odd.CkSimilar to int_div_ties_odd, but operates on references \xe2\x80\xa6101100BdCalls `NumInt::int_ref_subfactorial.CkSimilar to int_subfactorial, but operates on references \xe2\x80\xa6101100E`Returns <code>true</code> if the set shares any bit with <code>CAPS_LOCK</code>.E`Returns <code>true</code> if the set shares any bit with <code>CELL_GRID</code>.E`Returns <code>true</code> if the set shares any bit with <code>COMPOSING</code>.E`Returns <code>true</code> if the set shares any bit with <code>ENCIRCLED</code>.E`Returns <code>true</code> if the set shares any bit with <code>HEX_DIGIT</code>.E`Returns <code>true</code> if the set shares any bit with <code>Interrupt</code>.E`Returns <code>true</code> if the set shares any bit with <code>MAY_BLOCK</code>.E`Returns <code>true</code> if the set shares any bit with <code>MOUSE_SGR</code>.E`Returns <code>true</code> if the set shares any bit with <code>PRINTABLE</code>.E`Returns <code>true</code> if the set shares any bit with <code>REPEATING</code>.E`Returns <code>true</code> if the set shares any bit with <code>SIGSTKFLT</code>.E`Returns <code>true</code> if the set shares any bit with <code>SIGVTALRM</code>.E`Returns <code>true</code> if the set shares any bit with <code>SUBSCRIPT</code>.E`Returns <code>true</code> if the set shares any bit with <code>Terminate</code>.E`Returns <code>true</code> if the set shares any bit with <code>UNDERLINE</code>.0EdUnwraps this <code>Pin&lt;Ptr&gt;</code>, returning the underlying <code>Ptr</code>.ClReturns <code>true</code> for <code>InvalidParameter</code>.CeReturns <code>true</code> if the field value is zero.EcReturns <code>true</code> if all bits in the checked <code>[start..=end]</code> \xe2\x80\xa600000DdReturns <code>true</code> if this byte is a UTF-8 continuation byte.CeReturns the length of the stored data as a usize, \xe2\x80\xa60CmReturns the expected UTF-8 byte length based on the given \xe2\x80\xa6DoReturns a mutable reference to the <strong>linear-light perceptual</strong> \xe2\x80\xa60000000DbLoads the <code>f64</code> reference into all lanes of a register.DkLoad an <code>f64</code> and splat it to all lanes of an <code>m256d</code>DiLoad an <code>m128</code> and splat it to the lower and upper half of \xe2\x80\xa6BdLoads the reference into a register.0BfLoad data from memory into a register.0CkConverts this string to its ASCII lower case equivalent \xe2\x80\xa6CkConverts this string to its ASCII upper case equivalent \xe2\x80\xa6DgComputes the non-negative modulo of <code>self + other</code> over |\xe2\x80\xa60DoComputes the modulo of <code>self + other</code> over <code>modulus</code>, \xe2\x80\xa6101000110E`Computes <code>self / other</code> over <code>modulus</code>, unchecked version.EbComputes <code>self / other</code> over |<code>modulus</code>|, unchecked version.0100011101333332322222EaComputes the modulo of <code>self - other</code> over |<code>modulus</code>|, \xe2\x80\xa6DoComputes the modulo of <code>self - other</code> over <code>modulus</code>, \xe2\x80\xa61010011001EfReturns the middle sub-<code>string</code> with the given maximum <code>len</code> \xe2\x80\xa6EaGenerates a unique <code>IdSeqUsizeExample</code> ID with <code>SeqCst</code> \xe2\x80\xa6CmReturns the next 8-bit Unicode scalar, without performing \xe2\x80\xa6ChAdvances the state machine with the given code point \xe2\x80\xa6CmReturns the next Unicode scalar, without performing UTF-8 \xe2\x80\xa6CkCalculates the distance between two pointers within the \xe2\x80\xa6BaOpens the default capture device.CoDrops the top <code>n</code> stack elements, unchecked version.ClRotates the top three stack elements, counter-clockwise, \xe2\x80\xa6DlSaturating convert <code>i16</code> to <code>i8</code>, and pack the values.0DlSaturating convert <code>i16</code> to <code>u8</code>, and pack the values.0DiApproximates the partial derivative of the 2D function <code>f</code> \xe2\x80\xa6000000000CgCount the number of bits set within an <code>i32</code>CgCount the number of bits set within an <code>i64</code>Be\xe2\x85\x80 The prime number theorem formula.00ChUltra-fast stdout write. Panics if not all bytes are \xe2\x80\xa6DgAppends as many elements copied from <code>src</code> as fit at the \xe2\x80\xa6DgAppends as many copied elements from <code>src</code> as fit at the \xe2\x80\xa6BmUnchecked version of <code>push_front</code>.CmAppends an element and returns a reference to it if there \xe2\x80\xa6ClAttempts to construct this type by drawing seed material \xe2\x80\xa6000DkReturns an exclusive subslice starting from the given <code>start</code>\xe2\x80\xa6DeReturns an exclusive subslice up to the given <code>end</code> index.DfReturns an exclusive substring up to the given <code>end</code> index.CjReads all available bytes from stdin into an allocated \xe2\x80\xa6CbLanewise <code>1.0 / sqrt(a)</code> approximation.BeReciprocal of <code>f32</code> lanes.DfReturns the number of remaining IDs with <code>SeqCst</code> ordering.AfRemoves control flags.CnReplaces the byte-frame storage with an initialized vector \xe2\x80\xa6DjRotates the vector such that the first <code>OFFSET</code> elements of \xe2\x80\xa6Do\xe2\x8c\x97 <code>core</code> Hints the compiler that the <code>condition</code> is \xe2\x80\xa600DbSets <code>CLEAR_ON_DROP</code> if <code>condition</code> is true.BlSets the maximum known terminal color depth.DbSets <code>CRYPTOGRAPHIC</code> if <code>condition</code> is true.DbSets <code>DETERMINISTIC</code> if <code>condition</code> is true.BcSets <code>MOUSE_SGR_PIXELS</code>.CiReplaces the field value, masking it to fit the field \xe2\x80\xa6DgSets the value of the <code>IP_MULTICAST_TTL</code> option for this \xe2\x80\xa6BcSets <code>PROCESSED_OUTPUT</code>.2BiSets noncanonical read blocking behavior.BcSets <code>UNDERLINE_DOUBLE</code>.DkShifts the vector elements to the right by <code>OFFSET</code>, filling \xe2\x80\xa6EnApplies the sign of <code>i16</code> values in <code>b</code> to the values in <code>a</code>.DbLanewise <code>a * signum(b)</code> with lanes as <code>i16</code>EnApplies the sign of <code>i32</code> values in <code>b</code> to the values in <code>a</code>.DbLanewise <code>a * signum(b)</code> with lanes as <code>i32</code>CnSkips bytes until the next byte belonging to <code>set</code>.CjCreates a non-null raw slice from a thin pointer and a \xe2\x80\xa6BnForms a raw slice from a pointer and a length.BhStores the value to the reference given.0BgStore data from a register into memory.DgSubtract each lane horizontally, pack the outputs as <code>a</code> \xe2\x80\xa6BiSubtract adjacent <code>f64</code> lanes.BmUnchecked version of <code>swap2_back</code>.BmUnchecked version of <code>swap_front</code>.DmReturns the symmetric difference of <code>self</code> and <code>other</code>.ClVisits the values representing the symmetric difference, \xe2\x80\xa6CnVisits the elements representing the symmetric difference, \xe2\x80\xa6222222222222222222CgReturns the first <code>n</code> elements of the slice.ClConsumes and returns the range up to, but excluding, the \xe2\x80\xa6DgReturns the forward elapsed difference from <code>earlier</code> to \xe2\x80\xa600BiConverts <code>elapsed</code> to seconds.0000000ClConverts to an I Ching hexagram character using inverted \xe2\x80\xa6DdReturns a subslice without the given leading <code>byte</code>s, \xe2\x80\xa6CnImmutably borrows the wrapped value, returning an error if \xe2\x80\xa6CmConstructs a new boxed slice with uninitialized contents. \xe2\x80\xa6CmConstructs a new boxed slice with uninitialized contents, \xe2\x80\xa6CiReplaces the byte-frame storage and copies the active \xe2\x80\xa6BeReplaces the field value, if it fits.EaTransitions the terminal from <code>from</code> styles to <code>to</code> styles.DjConstructs a new, empty <code>Vec&lt;T, A&gt;</code> with at least the \xe2\x80\xa6DjReturns <code>self</code> with the checked field value replaced, if it \xe2\x80\xa60000EfUnpack and interleave high <code>i8</code> lanes of <code>a</code> and <code>b</code>.0EfUnpack and interleave low <code>i16</code> lanes of <code>a</code> and <code>b</code>.0EfUnpack and interleave low <code>i32</code> lanes of <code>a</code> and <code>b</code>.0EfUnpack and interleave low <code>i64</code> lanes of <code>a</code> and <code>b</code>.0BcUnsets <code>CLEAR_ON_ENTER</code>.BcUnsets <code>GENERATOR_WEAK</code>.EhReturns the contained <code>Err</code> value, consuming the <code>self</code> value, \xe2\x80\xa6DiUnwraps the contained <code>Ok(value)</code> or panics with the given \xe2\x80\xa6BoVisits each complete interleaved frame mutably.BmVisits each per-channel sample plane mutably.EgCreates an owned <code>PathBuf</code> like <code>self</code> but with the extension \xe2\x80\xa6DeReturns <code>self</code> with <code>BRACKETED_PASTE</code> inserted.DjReturns <code>self</code> with the field value replaced, masking it to \xe2\x80\xa6FbReturns <code>self</code> with <code>ENTROPY_WEAK</code> inserted if <code>condition</code> is \xe2\x80\xa6FbReturns <code>self</code> with <code>FOCUS_EVENTS</code> inserted if <code>condition</code> is \xe2\x80\xa6FbReturns <code>self</code> with <code>MOUSE_MOTION</code> inserted if <code>condition</code> is \xe2\x80\xa6FbReturns <code>self</code> with <code>PROPORTIONAL</code> inserted if <code>condition</code> is \xe2\x80\xa6FbReturns <code>self</code> with <code>REPRODUCIBLE</code> inserted if <code>condition</code> is \xe2\x80\xa6FbReturns <code>self</code> with <code>SA_NOCLDSTOP</code> inserted if <code>condition</code> is \xe2\x80\xa6FbReturns <code>self</code> with <code>SA_NOCLDWAIT</code> inserted if <code>condition</code> is \xe2\x80\xa6FbReturns <code>self</code> with <code>SA_RESETHAND</code> inserted if <code>condition</code> is \xe2\x80\xa6DaReturns <code>self</code> with <code>ENTROPY_WEAK</code> removed.DaReturns <code>self</code> with <code>FOCUS_EVENTS</code> removed.DaReturns <code>self</code> with <code>MOUSE_MOTION</code> removed.DaReturns <code>self</code> with <code>PROPORTIONAL</code> removed.DaReturns <code>self</code> with <code>REPRODUCIBLE</code> removed.DaReturns <code>self</code> with <code>SA_NOCLDSTOP</code> removed.DaReturns <code>self</code> with <code>SA_NOCLDWAIT</code> removed.DaReturns <code>self</code> with <code>SA_RESETHAND</code> removed.DkCasts <code>self</code> to <code>i16</code> wrapping at the numeric bounds.00DoCasts from <code>i16</code> to <code>i16</code> wrapping at the numeric bounds.DnCasts from <code>i8</code> to <code>i16</code> wrapping at the numeric bounds.E`Casts from <code>u128</code> to <code>i16</code> wrapping at the numeric bounds.EaCasts from <code>usize</code> to <code>i16</code> wrapping at the numeric bounds.DoCasts from <code>i32</code> to <code>i16</code> wrapping at the numeric bounds.DoCasts from <code>u32</code> to <code>i16</code> wrapping at the numeric bounds.DoCasts from <code>u64</code> to <code>i16</code> wrapping at the numeric bounds.DoCasts from <code>i64</code> to <code>i16</code> wrapping at the numeric bounds.DnCasts from <code>u8</code> to <code>i16</code> wrapping at the numeric bounds.DoCasts from <code>u16</code> to <code>i16</code> wrapping at the numeric bounds.E`Casts from <code>i128</code> to <code>i16</code> wrapping at the numeric bounds.EaCasts from <code>isize</code> to <code>i16</code> wrapping at the numeric bounds.DkCasts <code>self</code> to <code>i32</code> wrapping at the numeric bounds.00DnCasts from <code>i8</code> to <code>i32</code> wrapping at the numeric bounds.DnCasts from <code>u8</code> to <code>i32</code> wrapping at the numeric bounds.DoCasts from <code>u16</code> to <code>i32</code> wrapping at the numeric bounds.DoCasts from <code>i16</code> to <code>i32</code> wrapping at the numeric bounds.DoCasts from <code>u32</code> to <code>i32</code> wrapping at the numeric bounds.DoCasts from <code>i64</code> to <code>i32</code> wrapping at the numeric bounds.EaCasts from <code>isize</code> to <code>i32</code> wrapping at the numeric bounds.E`Casts from <code>u128</code> to <code>i32</code> wrapping at the numeric bounds.E`Casts from <code>i128</code> to <code>i32</code> wrapping at the numeric bounds.DoCasts from <code>i32</code> to <code>i32</code> wrapping at the numeric bounds.DoCasts from <code>u64</code> to <code>i32</code> wrapping at the numeric bounds.EaCasts from <code>usize</code> to <code>i32</code> wrapping at the numeric bounds.DkCasts <code>self</code> to <code>i64</code> wrapping at the numeric bounds.00DoCasts from <code>u16</code> to <code>i64</code> wrapping at the numeric bounds.EaCasts from <code>isize</code> to <code>i64</code> wrapping at the numeric bounds.DoCasts from <code>i16</code> to <code>i64</code> wrapping at the numeric bounds.E`Casts from <code>i128</code> to <code>i64</code> wrapping at the numeric bounds.DnCasts from <code>i8</code> to <code>i64</code> wrapping at the numeric bounds.DoCasts from <code>u32</code> to <code>i64</code> wrapping at the numeric bounds.DoCasts from <code>i32</code> to <code>i64</code> wrapping at the numeric bounds.DnCasts from <code>u8</code> to <code>i64</code> wrapping at the numeric bounds.EaCasts from <code>usize</code> to <code>i64</code> wrapping at the numeric bounds.E`Casts from <code>u128</code> to <code>i64</code> wrapping at the numeric bounds.DoCasts from <code>u64</code> to <code>i64</code> wrapping at the numeric bounds.DoCasts from <code>i64</code> to <code>i64</code> wrapping at the numeric bounds.DkCasts <code>self</code> to <code>u16</code> wrapping at the numeric bounds.00DoCasts from <code>u64</code> to <code>u16</code> wrapping at the numeric bounds.DnCasts from <code>u8</code> to <code>u16</code> wrapping at the numeric bounds.EaCasts from <code>isize</code> to <code>u16</code> wrapping at the numeric bounds.E`Casts from <code>u128</code> to <code>u16</code> wrapping at the numeric bounds.DoCasts from <code>u32</code> to <code>u16</code> wrapping at the numeric bounds.DoCasts from <code>i64</code> to <code>u16</code> wrapping at the numeric bounds.EaCasts from <code>usize</code> to <code>u16</code> wrapping at the numeric bounds.DoCasts from <code>i16</code> to <code>u16</code> wrapping at the numeric bounds.DoCasts from <code>u16</code> to <code>u16</code> wrapping at the numeric bounds.E`Casts from <code>i128</code> to <code>u16</code> wrapping at the numeric bounds.DnCasts from <code>i8</code> to <code>u16</code> wrapping at the numeric bounds.DoCasts from <code>i32</code> to <code>u16</code> wrapping at the numeric bounds.DkCasts <code>self</code> to <code>u32</code> wrapping at the numeric bounds.00E`Casts from <code>i128</code> to <code>u32</code> wrapping at the numeric bounds.DoCasts from <code>u64</code> to <code>u32</code> wrapping at the numeric bounds.DoCasts from <code>i32</code> to <code>u32</code> wrapping at the numeric bounds.DoCasts from <code>u32</code> to <code>u32</code> wrapping at the numeric bounds.EaCasts from <code>isize</code> to <code>u32</code> wrapping at the numeric bounds.DnCasts from <code>i8</code> to <code>u32</code> wrapping at the numeric bounds.DnCasts from <code>u8</code> to <code>u32</code> wrapping at the numeric bounds.E`Casts from <code>u128</code> to <code>u32</code> wrapping at the numeric bounds.DoCasts from <code>u16</code> to <code>u32</code> wrapping at the numeric bounds.EaCasts from <code>usize</code> to <code>u32</code> wrapping at the numeric bounds.DoCasts from <code>i64</code> to <code>u32</code> wrapping at the numeric bounds.DoCasts from <code>i16</code> to <code>u32</code> wrapping at the numeric bounds.DkCasts <code>self</code> to <code>u64</code> wrapping at the numeric bounds.00DnCasts from <code>u8</code> to <code>u64</code> wrapping at the numeric bounds.DoCasts from <code>i64</code> to <code>u64</code> wrapping at the numeric bounds.E`Casts from <code>u128</code> to <code>u64</code> wrapping at the numeric bounds.E`Casts from <code>i128</code> to <code>u64</code> wrapping at the numeric bounds.DnCasts from <code>i8</code> to <code>u64</code> wrapping at the numeric bounds.DoCasts from <code>i32</code> to <code>u64</code> wrapping at the numeric bounds.DoCasts from <code>i16</code> to <code>u64</code> wrapping at the numeric bounds.DoCasts from <code>u16</code> to <code>u64</code> wrapping at the numeric bounds.EaCasts from <code>isize</code> to <code>u64</code> wrapping at the numeric bounds.DoCasts from <code>u64</code> to <code>u64</code> wrapping at the numeric bounds.DoCasts from <code>u32</code> to <code>u64</code> wrapping at the numeric bounds.EaCasts from <code>usize</code> to <code>u64</code> wrapping at the numeric bounds.Cf\xf0\x9f\x92\xb1 \xf0\x9f\x9a\xa9 A failed conversion between two error types.00Bg<code>500 Internal Server Error</code>.Bg<code>428 Precondition Required</code>.Dg\xe2\x8c\x97 \xf0\x9f\x92\xaf <code>core</code> A simple wrapper around a type to assert \xe2\x80\xa600CfQuery alternate screen buffer mode 1047 support/state.ClQuery alternate scroll mode, affecting wheel behavior in \xe2\x80\xa6BiQuery bracketed paste mode support/state.Bg<code>416 Range Not Satisfiable</code>.Di<code>U+FFFD REPLACEMENT CHARACTER (\xef\xbf\xbd)</code> is used in Unicode to \xe2\x80\xa600C`Block magic-link traversal (procfs-style links).Ce\xf0\x9f\x8d\x83 \xf0\x9f\x9a\xa9 <code>core</code> An error returned by [\xe2\x80\xa600BmThreshold for sRGB linear segment (0.0031308)0ClInline capacity that usually makes this safe enum-backed \xe2\x80\xa6Ck\xf0\x9f\x95\x98 A time split from hours to nanoseconds, normalized \xe2\x80\xa600Cj\xf0\x9f\xa7\xaa Allows a site to access storage without top-level \xe2\x80\xa600CkCode to set double underline using the ECMA-48 SGR 21 form.Bg<code>422 Unprocessable Content</code>.Ck\xe3\x80\xb0\xef\xb8\x8f Trait for lossy compression of wavelet coefficients.00ClFormats the number into a provided buffer and returns it \xe2\x80\xa60000000000000CnConverts the number into an owned fixed-size string, using \xe2\x80\xa60000000000000Cf\xf0\x9f\xa7\xb5 \xe2\x9a\x9b\xef\xb8\x8f <code>core</code> A compiler memory fence.00CnReturns an estimate of the default amount of parallelism a \xe2\x80\xa6000EdCounts the number of 0s in <code>self</code> from the <code>[start..=end]</code> \xe2\x80\xa600EeGets the bits in <code>self</code> from the checked <code>[start..=end]</code> range.00EhSets the bits in <code>self</code> to 1, from the checked <code>[start..=end]</code> \xe2\x80\xa600CoCasts <code>self</code> to <code>isize</code> with range check.00DeCasts from <code>isize</code> to <code>isize</code> with range check.DcCasts from <code>i16</code> to <code>isize</code> with range check.DcCasts from <code>u64</code> to <code>isize</code> with range check.DcCasts from <code>i32</code> to <code>isize</code> with range check.DdCasts from <code>u128</code> to <code>isize</code> with range check.DcCasts from <code>u32</code> to <code>isize</code> with range check.DcCasts from <code>u16</code> to <code>isize</code> with range check.DdCasts from <code>i128</code> to <code>isize</code> with range check.DbCasts from <code>i8</code> to <code>isize</code> with range check.DbCasts from <code>u8</code> to <code>isize</code> with range check.DcCasts from <code>i64</code> to <code>isize</code> with range check.DeCasts from <code>usize</code> to <code>isize</code> with range check.CoCasts <code>self</code> to <code>usize</code> with range check.00DcCasts from <code>i64</code> to <code>usize</code> with range check.DdCasts from <code>i128</code> to <code>usize</code> with range check.DcCasts from <code>i32</code> to <code>usize</code> with range check.DeCasts from <code>usize</code> to <code>usize</code> with range check.DcCasts from <code>u32</code> to <code>usize</code> with range check.DeCasts from <code>isize</code> to <code>usize</code> with range check.DcCasts from <code>u64</code> to <code>usize</code> with range check.DbCasts from <code>i8</code> to <code>usize</code> with range check.DcCasts from <code>i16</code> to <code>usize</code> with range check.DcCasts from <code>u16</code> to <code>usize</code> with range check.DdCasts from <code>u128</code> to <code>usize</code> with range check.DbCasts from <code>u8</code> to <code>usize</code> with range check.AgClears the field value.ClLanewise <code>a == b</code> with lanes as <code>i16</code>.CiCompare <code>i16</code> lanes for equality, mask output.ClLanewise <code>a == b</code> with lanes as <code>i32</code>.CiCompare <code>i32</code> lanes for equality, mask output.ClLanewise <code>a == b</code> with lanes as <code>i64</code>.CiCompare <code>i64</code> lanes for equality, mask output.DbLanewise <code>a &amp;gt; b</code> with lanes as <code>i16</code>.DjCompare <code>i16</code> lanes for <code>a &amp;gt; b</code>, mask output.DbLanewise <code>a &amp;gt; b</code> with lanes as <code>i32</code>.DjCompare <code>i32</code> lanes for <code>a &amp;gt; b</code>, mask output.DbLanewise <code>a &amp;gt; b</code> with lanes as <code>i64</code>.DjCompare <code>i64</code> lanes for <code>a &amp;gt; b</code>, mask output.DbLanewise <code>a &amp;lt; b</code> with lanes as <code>i16</code>.DbLanewise <code>a &amp;lt; b</code> with lanes as <code>i32</code>.ClLanewise <code>(!a.is_nan()) &amp;amp; (!b.is_nan())</code>.DeLow lane <code>a.is_nan() | b.is_nan()</code>, other lanes unchanged.CeEncodes bounded 2D coordinates in column-major order.CeEncodes bounded 3D coordinates in column-major order.DkStores a value into the <code>Atomic</code> if the current value is the \xe2\x80\xa6DiStores a value into the <code>bool</code> if the current value is the \xe2\x80\xa6CkStores a value into the pointer if the current value is \xe2\x80\xa6CiStores a value into the atomic integer if the current \xe2\x80\xa60000000CmStores a value into the atomic float if the current value \xe2\x80\xa611011CfConfigures this PCM stream for interleaved read/write.DhThe nearest integer to itself, rounding ties away from <code>0.0</code>.0DgReturns the nearest integer to <code>x</code>, rounding ties to the \xe2\x80\xa60DoReturns <code>true</code> if all bits in <code>ENTROPY_WEAK</code> are present.DoReturns <code>true</code> if all bits in <code>FOCUS_EVENTS</code> are present.DoReturns <code>true</code> if all bits in <code>MOUSE_MOTION</code> are present.DoReturns <code>true</code> if all bits in <code>PROPORTIONAL</code> are present.DoReturns <code>true</code> if all bits in <code>REPRODUCIBLE</code> are present.DoReturns <code>true</code> if all bits in <code>SA_NOCLDSTOP</code> are present.DoReturns <code>true</code> if all bits in <code>SA_NOCLDWAIT</code> are present.DoReturns <code>true</code> if all bits in <code>SA_RESETHAND</code> are present.BaDecodes the given surrogate pair.CkReturns the determinant without checking matrix squareness.0000000Dh(addEventListener) Attaches a JavaScript function <code>event</code> \xe2\x80\xa6Df(removeEventListener) Removes a a Rust function <code>event</code> \xe2\x80\xa6CgExtends the front of the destaque from an iterator, \xe2\x80\xa6DhGets the <code>i32</code> lane requested. Only the lowest 2 bits are \xe2\x80\xa6DdGets the <code>i64</code> lane requested. Only the lowest bit is \xe2\x80\xa6D`Finds the index of the first 0 in <code>self</code> from the \xe2\x80\xa600000CiMarks the struct as non-exhaustive, indicating to the \xe2\x80\xa6CkMarks the tuple struct as non-exhaustive, indicating to \xe2\x80\xa6CmMarks the set as non-exhaustive, indicating to the reader \xe2\x80\xa6CnMarks the list as non-exhaustive, indicating to the reader \xe2\x80\xa6CmMarks the map as non-exhaustive, indicating to the reader \xe2\x80\xa6DaConditionally flips bits in a range if <code>cond</code> is true.00000Bg<em>const</em> method equivalent to \xe2\x80\xa600EbCreates a new <code>MaybeNiche</code> from a <code>usize</code>, saturating at \xe2\x80\xa600000000000000000000000000000000000000000000000EnConverts a <code>Vec&lt;u8&gt;</code> to a <code>String</code>, substituting invalid UTF-8 \xe2\x80\xa6CnLow lane fused <code>(a * b) + c</code>, other lanes unchangedDdLanewise fused <code>(a * b) addsub c</code> (adds odd lanes and \xe2\x80\xa60CoLow lane fused <code>(a * b) - c</code>, other lanes unchanged.DiLanewise fused <code>(a * b) subadd c</code> (subtracts odd lanes and \xe2\x80\xa60CjReturns the glyph for the specified Unicode scalar, if \xe2\x80\xa6DiInserts the default value into the option if it is <code>None</code>, \xe2\x80\xa6EeReturns <code>true</code> if the UTF-8 sequence starting at <code>index</code> is \xe2\x80\xa6CdA wrapper over <code>has_overlong_encoding()</code>.Cm(replaceState) Modifies the current history entry without \xe2\x80\xa6CcDoes this clock include time during system suspend?CcInserts an <code>m128d</code> to <code>m256d</code>EcInserts an <code>m128i</code> to an <code>m256i</code> at the high or low position.ChReturns the digital root in the given <code>base</code>.000CjWrites the prime factors in the given <code>buffer</code>.000EeComputes the modulo of <code>self + other</code> over |<code>modulus</code>|, and \xe2\x80\xa60000000EeComputes the modulo of <code>self - other</code> over |<code>modulus</code>|, and \xe2\x80\xa6000BeCalls `NumInt::int_ref_div_ties_away.ClSimilar to int_div_ties_away, but operates on references \xe2\x80\xa6101100BeCalls `NumInt::int_ref_div_ties_even.ClSimilar to int_div_ties_even, but operates on references \xe2\x80\xa6101100BeCalls `NumInt::int_ref_factors_prime.ClSimilar to int_factors_prime, but operates on references \xe2\x80\xa6101100BeCalls `NumInt::int_ref_modulo_cycles.ClSimilar to int_modulo_cycles, but operates on references \xe2\x80\xa6101100EaReturns <code>true</code> if the set shares any bit with <code>ALT_SCREEN</code>.EaReturns <code>true</code> if the set shares any bit with <code>BLINK_FAST</code>.EaReturns <code>true</code> if the set shares any bit with <code>COLOR_PAIR</code>.EaReturns <code>true</code> if the set shares any bit with <code>IDENT_HEAD</code>.EaReturns <code>true</code> if the set shares any bit with <code>IDENT_TAIL</code>.EaReturns <code>true</code> if the set shares any bit with <code>MOUSE_DRAG</code>.EaReturns <code>true</code> if the set shares any bit with <code>SA_NODEFER</code>.EaReturns <code>true</code> if the set shares any bit with <code>SA_ONSTACK</code>.EaReturns <code>true</code> if the set shares any bit with <code>SA_RESTART</code>.EaReturns <code>true</code> if the set shares any bit with <code>SA_SIGINFO</code>.DcDecomposes a <code>Vec&lt;T&gt;</code> into its raw components: \xe2\x80\xa6ClReturns <code>true</code> for <code>InvalidKeyLength</code>.DfReturns <code>true</code> if the address is a unicast address with \xe2\x80\xa6CnIterator over generated IDs with <code>SeqCst</code> ordering.DjLoads the reference given and zeroes any <code>i32</code> lanes not in \xe2\x80\xa60DjLoads the reference given and zeroes any <code>i64</code> lanes not in \xe2\x80\xa60DkReads contiguous elements from <code>slice</code>. Each element is read \xe2\x80\xa6C`<code>macro_rules_attribute</code> modificationsB`Returns metadata for this field.CnReturns the next 16-bit Unicode scalar, without performing \xe2\x80\xa6CmReturns the next Unicode scalar, without performing UTF-8 \xe2\x80\xa6DcCreates a <code>PtrNonNull</code> with the given address and no \xe2\x80\xa6BbOpens the default playback device.CnRotates the top six stack elements, counter-clockwise, two \xe2\x80\xa6DmSaturating convert <code>i32</code> to <code>i16</code>, and pack the values.0DmSaturating convert <code>i32</code> to <code>u16</code>, and pack the values.0CgReturns the physical size of the window in hardware \xe2\x80\xa6DcLike <code>print_bytes</code>, but panics on failure instead of \xe2\x80\xa6CnPrepends as many copied elements from <code>src</code> as fit.DhPrepends as many copied elements from <code>src</code> as fit at the \xe2\x80\xa6DeAppends all copied elements from <code>src</code>, or returns the \xe2\x80\xa6DaAppends all copied elements from <code>src</code>, or none if \xe2\x80\xa600=C`Chooses one valid item using reservoir sampling.000BbReturns the stored bits per pixel.000BfReturns the stored bytes per scanline.000000DhReverses the order of the bits in <code>self</code> from the checked \xe2\x80\xa600000DkRotates the vector such that the first <code>self.len() - OFFSET</code> \xe2\x80\xa6CbEncodes bounded 2D coordinates in row-major order.CbEncodes bounded 3D coordinates in row-major order.DjCasts <code>self</code> to <code>i8</code> clamping at the numeric bounds.00DmCasts from <code>u8</code> to <code>i8</code> clamping at the numeric bounds.DnCasts from <code>u64</code> to <code>i8</code> clamping at the numeric bounds.DnCasts from <code>u16</code> to <code>i8</code> clamping at the numeric bounds.E`Casts from <code>isize</code> to <code>i8</code> clamping at the numeric bounds.DnCasts from <code>i32</code> to <code>i8</code> clamping at the numeric bounds.DnCasts from <code>i16</code> to <code>i8</code> clamping at the numeric bounds.DnCasts from <code>u32</code> to <code>i8</code> clamping at the numeric bounds.DmCasts from <code>i8</code> to <code>i8</code> clamping at the numeric bounds.DoCasts from <code>u128</code> to <code>i8</code> clamping at the numeric bounds.DoCasts from <code>i128</code> to <code>i8</code> clamping at the numeric bounds.E`Casts from <code>usize</code> to <code>i8</code> clamping at the numeric bounds.DnCasts from <code>i64</code> to <code>i8</code> clamping at the numeric bounds.DjCasts <code>self</code> to <code>u8</code> clamping at the numeric bounds.00DnCasts from <code>u16</code> to <code>u8</code> clamping at the numeric bounds.DnCasts from <code>u64</code> to <code>u8</code> clamping at the numeric bounds.E`Casts from <code>usize</code> to <code>u8</code> clamping at the numeric bounds.DnCasts from <code>i32</code> to <code>u8</code> clamping at the numeric bounds.DnCasts from <code>i16</code> to <code>u8</code> clamping at the numeric bounds.DnCasts from <code>u32</code> to <code>u8</code> clamping at the numeric bounds.DoCasts from <code>i128</code> to <code>u8</code> clamping at the numeric bounds.DmCasts from <code>u8</code> to <code>u8</code> clamping at the numeric bounds.DoCasts from <code>u128</code> to <code>u8</code> clamping at the numeric bounds.E`Casts from <code>isize</code> to <code>u8</code> clamping at the numeric bounds.DnCasts from <code>i64</code> to <code>u8</code> clamping at the numeric bounds.DmCasts from <code>i8</code> to <code>u8</code> clamping at the numeric bounds.BeEnters a scoped termios state update.DcSets <code>CLEAR_ON_ENTER</code> if <code>condition</code> is true.DcSets <code>GENERATOR_WEAK</code> if <code>condition</code> is true.DhSets the value of the <code>IP_MULTICAST_LOOP</code> option for this \xe2\x80\xa6DjSets the value of the <code>IPV6_MULTICAST_LOOP</code> option for this \xe2\x80\xa6CdEnables or disables terminal output post-processing.DdSets the args into an <code>m128i</code>, first arg is the low lane.CiSet <code>i8</code> args into an <code>m256i</code> lane.EbSets the lowest lane of an <code>m128</code> as all lanes of an <code>m256</code>.DjReturns a mutable middle subslice of <code>slice</code> with the given \xe2\x80\xa600BhStores the value to the reference given.0BgStore data from a register into memory.0CmComputes the determinant of a square matrix using Laplace \xe2\x80\xa60000000BmUnchecked version of <code>swap2_back</code>.CiPrimitive-index variant of <code>swap_remove_copy</code>.00DeRemoves and returns a copied value at logical <code>index</code>, \xe2\x80\xa6CiPrimitive-index variant of <code>swap_remove_init</code>.CiPrimitive-index variant of <code>swap_remove_with</code>.CdConsumes and returns an ASCII identifier-tail range.D`Returns the last <code>n</code> elements of the exclusive slice.DaReturns the last <code>n</code> elements of the exclusive string.BeReturns if all masked bits are 0, \xe2\x80\xa6DeReturns a subslice without the given trailing <code>byte</code>s, \xe2\x80\xa6DhAllocates memory in the given allocator then clones <code>src</code> \xe2\x80\xa6EgConstructs a new <code>Rc&lt;T&gt;</code> with a clone of <code>value</code> in the \xe2\x80\xa6CbAppends <code>s</code> only if it fits completely.DbTries to append the complete <code>string</code> slice to the end.CiAppends the entire <code>string</code> or nothing at all.DjReturns <code>self</code> with the checked field value replaced, if it \xe2\x80\xa6EgUnpack and interleave high <code>i16</code> lanes of <code>a</code> and <code>b</code>.0EgUnpack and interleave high <code>i32</code> lanes of <code>a</code> and <code>b</code>.0EgUnpack and interleave high <code>i64</code> lanes of <code>a</code> and <code>b</code>.0Dh\xf0\x9f\x92\xaf <code>core</code> Informs the compiler that the current calling \xe2\x80\xa600BdUnsets <code>BRACKETED_PASTE</code>.EgUnwraps the result if the <code>Option</code> is <code>Some</code>, otherwise calls \xe2\x80\xa600B`Valid precision bits per sample.00BnWebAssembly pointer event callback dispatcher.FcReturns <code>self</code> with <code>CLEAR_ON_DROP</code> inserted if <code>condition</code> is \xe2\x80\xa6BjReturns a copy with the given color depth.FcReturns <code>self</code> with <code>CRYPTOGRAPHIC</code> inserted if <code>condition</code> is \xe2\x80\xa6FcReturns <code>self</code> with <code>DETERMINISTIC</code> inserted if <code>condition</code> is \xe2\x80\xa6DfReturns <code>self</code> with <code>MOUSE_SGR_PIXELS</code> inserted.DjReturns <code>self</code> with the field value replaced, masking it to \xe2\x80\xa6DfReturns <code>self</code> with <code>PROCESSED_OUTPUT</code> inserted.1DfReturns <code>self</code> with <code>UNDERLINE_DOUBLE</code> inserted.DbReturns <code>self</code> with <code>CLEAR_ON_DROP</code> removed.DbReturns <code>self</code> with <code>CRYPTOGRAPHIC</code> removed.DbReturns <code>self</code> with <code>DETERMINISTIC</code> removed.DlCasts <code>self</code> to <code>i128</code> wrapping at the numeric bounds.00EaCasts from <code>u128</code> to <code>i128</code> wrapping at the numeric bounds.DoCasts from <code>i8</code> to <code>i128</code> wrapping at the numeric bounds.EaCasts from <code>i128</code> to <code>i128</code> wrapping at the numeric bounds.E`Casts from <code>i64</code> to <code>i128</code> wrapping at the numeric bounds.EbCasts from <code>usize</code> to <code>i128</code> wrapping at the numeric bounds.E`Casts from <code>u64</code> to <code>i128</code> wrapping at the numeric bounds.E`Casts from <code>u32</code> to <code>i128</code> wrapping at the numeric bounds.DoCasts from <code>u8</code> to <code>i128</code> wrapping at the numeric bounds.E`Casts from <code>u16</code> to <code>i128</code> wrapping at the numeric bounds.E`Casts from <code>i16</code> to <code>i128</code> wrapping at the numeric bounds.E`Casts from <code>i32</code> to <code>i128</code> wrapping at the numeric bounds.EbCasts from <code>isize</code> to <code>i128</code> wrapping at the numeric bounds.DlCasts <code>self</code> to <code>u128</code> wrapping at the numeric bounds.00E`Casts from <code>u32</code> to <code>u128</code> wrapping at the numeric bounds.E`Casts from <code>i64</code> to <code>u128</code> wrapping at the numeric bounds.E`Casts from <code>u64</code> to <code>u128</code> wrapping at the numeric bounds.E`Casts from <code>u16</code> to <code>u128</code> wrapping at the numeric bounds.DoCasts from <code>u8</code> to <code>u128</code> wrapping at the numeric bounds.EaCasts from <code>i128</code> to <code>u128</code> wrapping at the numeric bounds.DoCasts from <code>i8</code> to <code>u128</code> wrapping at the numeric bounds.E`Casts from <code>i32</code> to <code>u128</code> wrapping at the numeric bounds.E`Casts from <code>i16</code> to <code>u128</code> wrapping at the numeric bounds.EbCasts from <code>usize</code> to <code>u128</code> wrapping at the numeric bounds.EbCasts from <code>isize</code> to <code>u128</code> wrapping at the numeric bounds.EaCasts from <code>u128</code> to <code>u128</code> wrapping at the numeric bounds.CjCode to enable DEC alternate screen mode 1047, without \xe2\x80\xa6BdCode to enable bracketed paste mode.fU+001700Cd\xf0\x9f\x90\xa7 \xf0\x9f\xae\x96 \xe2\x8a\x86 <code>LinuxTermios</code> input flags.00Cd\xf0\x9f\x90\xa7 \xf0\x9f\xae\x96 \xe2\x8a\x86 <code>LinuxTermios</code> local flags.00Cg\xe2\x9c\xa8 \xf0\x9f\x97\x83\xef\xb8\x8f A <code>TypeId</code>-keyed static hashmap.00CfThe PCM sample format does not match the requested \xe2\x80\xa600CdQuery SGR mouse pixel-coordinate mode support/state.CkThe positioned grid exceeds the renderer\xe2\x80\x99s coordinate \xe2\x80\xa600E`Inline capacity that makes <code>StringU8&lt;CAP&gt;</code> the same size as \xe2\x80\xa6AjStale network file handle.0000Dh\xf0\x9f\xa7\xb5 <code>std</code> An owned permission to join on a scoped thread \xe2\x80\xa600Cg\xf0\x9f\x95\x98 A time split from milliseconds to nanoseconds, \xe2\x80\xa600Bh<code>415 Unsupported Media Type</code>.DoThe WAVEFORMATEX <code>fmt </code> payload length with <code>cbSize = 0</code>.DfPerform the last round of an AES decryption flow on <code>a</code> \xe2\x80\xa6DfPerform the last round of an AES encryption flow on <code>a</code> \xe2\x80\xa6CmProvides shared access to the key and owned access to the \xe2\x80\xa6EcFlips the bits in <code>self</code> from the checked <code>[start..=end]</code> \xe2\x80\xa600DiReturns a bitmask of ones from the checked <code>[start..=end]</code> \xe2\x80\xa600DdBlend the <code>i8</code> lanes according to a runtime varying mask.D`Blend <code>i8</code> lanes according to a runtime varying mask.BiCancels a request for an animation frame.D`Bit-preserving cast to <code>m128</code> from <code>m256</code>.CnReturns the amount of time elapsed from another instant to \xe2\x80\xa6AgClears the field value.0ClLanewise <code>(!a.is_nan()) &amp;amp; (!b.is_nan())</code>.DdLow lane <code>a.is_nan() | b.is_nan()</code>, other lane unchanged.DcWrites the color components to a pre-allocated <code>buffer</code>.00E`Returns <code>true</code> if all bits in <code>CLEAR_ON_DROP</code> are present.E`Returns <code>true</code> if all bits in <code>CRYPTOGRAPHIC</code> are present.E`Returns <code>true</code> if all bits in <code>DETERMINISTIC</code> are present.FkCopies <code>count * size_of::&lt;T&gt;()</code> bytes from <code>self</code> to <code>dest</code>. The \xe2\x80\xa6DjDecrements the strong reference count on the <code>Arc&lt;T&gt;</code> \xe2\x80\xa6DiDecrements the strong reference count on the <code>Rc&lt;T&gt;</code> \xe2\x80\xa6CkReturns the numeric value (0-9) of the decimal digit at \xe2\x80\xa600000CmReturns the numeric value (0-15) of the hexadecimal digit \xe2\x80\xa600000D`Forwards to the method defined on the type <code>dyn Any</code>.ClForwards to the method defined on the type <code>Any</code>.CjReturns a mutable reference to the inner value as type \xe2\x80\xa612DbReturns a reference to the inner value as type <code>dyn T</code>.Cd(addEventListener) Attaches a Rust function as a \xe2\x80\xa6CiExtracts an <code>i32</code> lane from <code>m256i</code>CiExtracts an <code>i64</code> lane from <code>m256i</code>CdExtracts an <code>m128</code> from <code>m256</code>DjCreates a <code>SixelChar</code> from any character, using the lower 6 \xe2\x80\xa6Bg<em>const</em> method equivalent to \xe2\x80\xa6CoConverts a timed normalized <code>EventMouse</code> back to \xe2\x80\xa6CoConverts a timed normalized <code>EventWheel</code> back to \xe2\x80\xa6CcCreates a sixel from an I Ching hexagram character.DdConverts integer milliseconds to <code>f32</code> and stores the \xe2\x80\xa644DdLanewise fused <code>(a * b) addsub c</code> (adds odd lanes and \xe2\x80\xa60BhLanewise fused <code>-(a * b) + c</code>0BhLanewise fused <code>-(a * b) - c</code>0DiLanewise fused <code>(a * b) subadd c</code> (subtracts odd lanes and \xe2\x80\xa60DhReturns the number of IDs generated so far with <code>Acquire</code> \xe2\x80\xa6DjAttempts to get mutable references to <code>N</code> values in the map \xe2\x80\xa60CnReturns an exclusive reference to the element at the given \xe2\x80\xa60DiIf the option is <code>None</code>, calls the closure and inserts its \xe2\x80\xa6AhReturns the field value.CfReturns a reference to the element at the given 2D \xe2\x80\xa60CjVerifies that the continuation bytes following a UTF-8 \xe2\x80\xa6CeA wrapper over <code>has_valid_continuation()</code>.EgCreates a mismatch where <code>need</code> is an <code>Interval::empty_with</code>, \xe2\x80\xa6DjIncrements the strong reference count on the <code>Arc&lt;T&gt;</code> \xe2\x80\xa6DiIncrements the strong reference count on the <code>Rc&lt;T&gt;</code> \xe2\x80\xa6DhWrites the proper factors in <code>fbuf</code>, and the unique prime \xe2\x80\xa6000BfCalls `NumInt::int_ref_factors_proper.CmSimilar to int_factors_proper, but operates on references \xe2\x80\xa6101100BfCalls `NumInt::int_ref_modulo_add_inv.CmSimilar to int_modulo_add_inv, but operates on references \xe2\x80\xa6101100BfCalls `NumInt::int_ref_modulo_mul_inv.CmSimilar to int_modulo_mul_inv, but operates on references \xe2\x80\xa6101100EbReturns <code>true</code> if the set shares any bit with <code>HIDE_CURSOR</code>.EbReturns <code>true</code> if the set shares any bit with <code>SA_RESTORER</code>.EbReturns <code>true</code> if the set shares any bit with <code>SCROLL_LOCK</code>.EbReturns <code>true</code> if the set shares any bit with <code>SUPERSCRIPT</code>.EbReturns <code>true</code> if the set shares any bit with <code>SYNC_UPDATE</code>.EbReturns <code>true</code> if the set shares any bit with <code>TOUCHSCREEN</code>.D`Returns <code>true</code> if the field value equals its maximum.EcReturns <code>true</code> if all bits in the checked <code>[start..=end]</code> \xe2\x80\xa600000CnReturns <code>true</code> for <code>VerificationFailed</code>.BoCount the leading zeroes in a <code>u32</code>.BoCount the leading zeroes in a <code>u64</code>.DjLoad an <code>m128d</code> and splat it to the lower and upper half of \xe2\x80\xa6CjLoads the reference into a register, replacing the low \xe2\x80\xa6DkReads contiguous elements from <code>slice</code>. Each element is read \xe2\x80\xa6CmMin <code>u16</code> value, position, and other lanes zeroed.EhLanewise <code>a * b</code> with lanes as <code>i16</code>, keep the low bits of the \xe2\x80\xa6DeMultiply the <code>i16</code> lanes and keep the low half of each \xe2\x80\xa6DeMultiply the <code>i32</code> lanes and keep the low half of each \xe2\x80\xa6EoMultiply the lower <code>i32</code> within each <code>i64</code> lane, <code>i64</code> output.EoMultiply the lower <code>u32</code> within each <code>u64</code> lane, <code>u64</code> output.EaGenerates a unique <code>IdSeqUsizeExample</code> ID with <code>AcqRel</code> \xe2\x80\xa6CkEnsures a value is in the entry by inserting, if empty, \xe2\x80\xa6DhDeposit contiguous low bits from a <code>u32</code> according to a mask.DhDeposit contiguous low bits from a <code>u64</code> according to a mask.CiExtract bits from a <code>u32</code> according to a mask.CiExtract bits from a <code>u64</code> according to a mask.ClUltra-fast stdout write with newline. Panics if write isn\xe2\x80\xa6CeProbes terminal capabilities, refreshes size, and \xe2\x80\xa6CfAttempts to choose one valid item using random scores.000DkReturns an exclusive subslice starting from the given <code>start</code>\xe2\x80\xa6CjReturns an exclusive substring starting from the given \xe2\x80\xa6CiReturns an exclusive subslice up to and including the \xe2\x80\xa6CjReturns an exclusive substring up to and including the \xe2\x80\xa6DeReturns an exclusive subslice up to the given <code>end</code> index.BcReturns the stored bytes per pixel.000BjReads the CPU\xe2\x80\x99s timestamp counter value.DbLow lane <code>1.0 / sqrt(a)</code> approximation, other lanes \xe2\x80\xa6DgReturns the number of remaining IDs with <code>Acquire</code> ordering.DkCasts <code>self</code> to <code>i16</code> clamping at the numeric bounds.00E`Casts from <code>i128</code> to <code>i16</code> clamping at the numeric bounds.DoCasts from <code>u16</code> to <code>i16</code> clamping at the numeric bounds.DoCasts from <code>i64</code> to <code>i16</code> clamping at the numeric bounds.DoCasts from <code>u32</code> to <code>i16</code> clamping at the numeric bounds.E`Casts from <code>u128</code> to <code>i16</code> clamping at the numeric bounds.DnCasts from <code>i8</code> to <code>i16</code> clamping at the numeric bounds.DoCasts from <code>i32</code> to <code>i16</code> clamping at the numeric bounds.EaCasts from <code>isize</code> to <code>i16</code> clamping at the numeric bounds.DoCasts from <code>i16</code> to <code>i16</code> clamping at the numeric bounds.DnCasts from <code>u8</code> to <code>i16</code> clamping at the numeric bounds.DoCasts from <code>u64</code> to <code>i16</code> clamping at the numeric bounds.EaCasts from <code>usize</code> to <code>i16</code> clamping at the numeric bounds.DkCasts <code>self</code> to <code>i32</code> clamping at the numeric bounds.00EaCasts from <code>isize</code> to <code>i32</code> clamping at the numeric bounds.DoCasts from <code>i64</code> to <code>i32</code> clamping at the numeric bounds.DoCasts from <code>u16</code> to <code>i32</code> clamping at the numeric bounds.EaCasts from <code>usize</code> to <code>i32</code> clamping at the numeric bounds.DnCasts from <code>u8</code> to <code>i32</code> clamping at the numeric bounds.DoCasts from <code>u32</code> to <code>i32</code> clamping at the numeric bounds.DoCasts from <code>i32</code> to <code>i32</code> clamping at the numeric bounds.DnCasts from <code>i8</code> to <code>i32</code> clamping at the numeric bounds.DoCasts from <code>i16</code> to <code>i32</code> clamping at the numeric bounds.DoCasts from <code>u64</code> to <code>i32</code> clamping at the numeric bounds.E`Casts from <code>u128</code> to <code>i32</code> clamping at the numeric bounds.E`Casts from <code>i128</code> to <code>i32</code> clamping at the numeric bounds.DkCasts <code>self</code> to <code>i64</code> clamping at the numeric bounds.00DoCasts from <code>i64</code> to <code>i64</code> clamping at the numeric bounds.EaCasts from <code>usize</code> to <code>i64</code> clamping at the numeric bounds.DoCasts from <code>i16</code> to <code>i64</code> clamping at the numeric bounds.EaCasts from <code>isize</code> to <code>i64</code> clamping at the numeric bounds.E`Casts from <code>u128</code> to <code>i64</code> clamping at the numeric bounds.E`Casts from <code>i128</code> to <code>i64</code> clamping at the numeric bounds.DoCasts from <code>u64</code> to <code>i64</code> clamping at the numeric bounds.DnCasts from <code>i8</code> to <code>i64</code> clamping at the numeric bounds.DoCasts from <code>u32</code> to <code>i64</code> clamping at the numeric bounds.DnCasts from <code>u8</code> to <code>i64</code> clamping at the numeric bounds.DoCasts from <code>u16</code> to <code>i64</code> clamping at the numeric bounds.DoCasts from <code>i32</code> to <code>i64</code> clamping at the numeric bounds.DkCasts <code>self</code> to <code>u16</code> clamping at the numeric bounds.00DnCasts from <code>i8</code> to <code>u16</code> clamping at the numeric bounds.E`Casts from <code>i128</code> to <code>u16</code> clamping at the numeric bounds.DoCasts from <code>i64</code> to <code>u16</code> clamping at the numeric bounds.DoCasts from <code>u64</code> to <code>u16</code> clamping at the numeric bounds.EaCasts from <code>isize</code> to <code>u16</code> clamping at the numeric bounds.DoCasts from <code>i16</code> to <code>u16</code> clamping at the numeric bounds.E`Casts from <code>u128</code> to <code>u16</code> clamping at the numeric bounds.DoCasts from <code>i32</code> to <code>u16</code> clamping at the numeric bounds.DoCasts from <code>u16</code> to <code>u16</code> clamping at the numeric bounds.DoCasts from <code>u32</code> to <code>u16</code> clamping at the numeric bounds.EaCasts from <code>usize</code> to <code>u16</code> clamping at the numeric bounds.DnCasts from <code>u8</code> to <code>u16</code> clamping at the numeric bounds.DkCasts <code>self</code> to <code>u32</code> clamping at the numeric bounds.00DnCasts from <code>u8</code> to <code>u32</code> clamping at the numeric bounds.DoCasts from <code>i32</code> to <code>u32</code> clamping at the numeric bounds.DnCasts from <code>i8</code> to <code>u32</code> clamping at the numeric bounds.E`Casts from <code>i128</code> to <code>u32</code> clamping at the numeric bounds.EaCasts from <code>isize</code> to <code>u32</code> clamping at the numeric bounds.DoCasts from <code>i16</code> to <code>u32</code> clamping at the numeric bounds.DoCasts from <code>u32</code> to <code>u32</code> clamping at the numeric bounds.DoCasts from <code>i64</code> to <code>u32</code> clamping at the numeric bounds.DoCasts from <code>u64</code> to <code>u32</code> clamping at the numeric bounds.DoCasts from <code>u16</code> to <code>u32</code> clamping at the numeric bounds.EaCasts from <code>usize</code> to <code>u32</code> clamping at the numeric bounds.E`Casts from <code>u128</code> to <code>u32</code> clamping at the numeric bounds.DkCasts <code>self</code> to <code>u64</code> clamping at the numeric bounds.00DoCasts from <code>u16</code> to <code>u64</code> clamping at the numeric bounds.DoCasts from <code>u32</code> to <code>u64</code> clamping at the numeric bounds.E`Casts from <code>u128</code> to <code>u64</code> clamping at the numeric bounds.EaCasts from <code>usize</code> to <code>u64</code> clamping at the numeric bounds.EaCasts from <code>isize</code> to <code>u64</code> clamping at the numeric bounds.DoCasts from <code>i64</code> to <code>u64</code> clamping at the numeric bounds.DoCasts from <code>i16</code> to <code>u64</code> clamping at the numeric bounds.DoCasts from <code>i32</code> to <code>u64</code> clamping at the numeric bounds.DnCasts from <code>u8</code> to <code>u64</code> clamping at the numeric bounds.DoCasts from <code>u64</code> to <code>u64</code> clamping at the numeric bounds.E`Casts from <code>i128</code> to <code>u64</code> clamping at the numeric bounds.DnCasts from <code>i8</code> to <code>u64</code> clamping at the numeric bounds.DdSets <code>BRACKETED_PASTE</code> if <code>condition</code> is true.CiReplaces the field value, masking it to fit the field \xe2\x80\xa6DdSets the args into an <code>m128i</code>, first arg is the low lane.CjSet <code>i16</code> args into an <code>m256i</code> lane.1CjSet <code>i32</code> args into an <code>m256i</code> lane.CjSet <code>i64</code> args into an <code>m256i</code> lane.CeSet <code>m128</code> args into an <code>m256</code>.DjReturns a mutable middle subslice of <code>slice</code> with the given \xe2\x80\xa600CgSplat the lowest 8-bit lane across the entire 128 bits.D`Stores the <code>i32</code> masked lanes given to the reference.000DfConditionally write contiguous elements to <code>slice</code>. The \xe2\x80\xa6CdReturns the number of stored bytes per pixel, if \xe2\x80\xa6DaReturns the first <code>n</code> elements of the exclusive slice.DbReturns the first <code>n</code> elements of the exclusive string.ClReturns the slice omitting the last <code>n</code> elements.CmReturns the string omitting the last <code>n</code> elements.ClConverts to a <code>Duration</code>, saturating on overflow.CnTruncate the <code>f32</code> lanes to <code>i32</code> lanes.BhReturns an unexpected-after-quote error.BeUnsets <code>MOUSE_SGR_PIXELS</code>.BeUnsets <code>PROCESSED_OUTPUT</code>.BeUnsets <code>UNDERLINE_DOUBLE</code>.FdReturns <code>self</code> with <code>CLEAR_ON_ENTER</code> inserted if <code>condition</code> is \xe2\x80\xa6FdReturns <code>self</code> with <code>GENERATOR_WEAK</code> inserted if <code>condition</code> is \xe2\x80\xa6DcReturns <code>self</code> with <code>CLEAR_ON_ENTER</code> removed.DcReturns <code>self</code> with <code>GENERATOR_WEAK</code> removed.DdCreates a pointer with the given address and no <em>provenance</em>.DmCasts <code>self</code> to <code>isize</code> wrapping at the numeric bounds.00EcCasts from <code>isize</code> to <code>isize</code> wrapping at the numeric bounds.E`Casts from <code>u8</code> to <code>isize</code> wrapping at the numeric bounds.EaCasts from <code>i16</code> to <code>isize</code> wrapping at the numeric bounds.EaCasts from <code>u32</code> to <code>isize</code> wrapping at the numeric bounds.EaCasts from <code>u16</code> to <code>isize</code> wrapping at the numeric bounds.EcCasts from <code>usize</code> to <code>isize</code> wrapping at the numeric bounds.EbCasts from <code>u128</code> to <code>isize</code> wrapping at the numeric bounds.EaCasts from <code>i64</code> to <code>isize</code> wrapping at the numeric bounds.EaCasts from <code>u64</code> to <code>isize</code> wrapping at the numeric bounds.EbCasts from <code>i128</code> to <code>isize</code> wrapping at the numeric bounds.E`Casts from <code>i8</code> to <code>isize</code> wrapping at the numeric bounds.EaCasts from <code>i32</code> to <code>isize</code> wrapping at the numeric bounds.DmCasts <code>self</code> to <code>usize</code> wrapping at the numeric bounds.00EaCasts from <code>u64</code> to <code>usize</code> wrapping at the numeric bounds.EaCasts from <code>u16</code> to <code>usize</code> wrapping at the numeric bounds.E`Casts from <code>u8</code> to <code>usize</code> wrapping at the numeric bounds.EcCasts from <code>isize</code> to <code>usize</code> wrapping at the numeric bounds.EcCasts from <code>usize</code> to <code>usize</code> wrapping at the numeric bounds.EaCasts from <code>i16</code> to <code>usize</code> wrapping at the numeric bounds.EaCasts from <code>u32</code> to <code>usize</code> wrapping at the numeric bounds.EbCasts from <code>i128</code> to <code>usize</code> wrapping at the numeric bounds.EaCasts from <code>i64</code> to <code>usize</code> wrapping at the numeric bounds.EbCasts from <code>u128</code> to <code>usize</code> wrapping at the numeric bounds.EaCasts from <code>i32</code> to <code>usize</code> wrapping at the numeric bounds.E`Casts from <code>i8</code> to <code>usize</code> wrapping at the numeric bounds.CiWrites all requested planar frames and drains pending \xe2\x80\xa6000CmWrites the sixel color definition bytes for a given color \xe2\x80\xa6CfWrites 0..=20 decimal digits without leading zeros \xe2\x80\xa6CnWrites 0..=3 decimal digits without leading zeros starting \xe2\x80\xa61CnWrites 0..=5 decimal digits without leading zeros starting \xe2\x80\xa6CfWrites 0..=39 decimal digits without leading zeros \xe2\x80\xa6CfWrites 0..=10 decimal digits without leading zeros \xe2\x80\xa6CiWrites 1..=2 hexadecimal digits without leading zeros \xe2\x80\xa6Cm\xe2\x9c\xa8 \xf0\x9f\x97\x83\xef\xb8\x8f A linear buffer view over contiguous storage, \xe2\x80\xa600Cm\xe2\x9c\xa8 \xf0\x9f\x97\x83\xef\xb8\x8f A static ring buffer over contiguous storage, \xe2\x80\xa600CgCode to restore the underline color to the terminal \xe2\x80\xa6CkCode to disable DEC alternate screen mode 1047, without \xe2\x80\xa6BeCode to disable bracketed paste mode.CiCode to enable SGR extended mouse encoding with pixel \xe2\x80\xa6fU+001F00fU+001E00ClThe WAVE extensible <code>fmt</code> extension is malformed.00B`\xe2\xac\xa1 \xf0\x9d\x90\x93 \xf0\x9f\xa7\xb1 Full Text Metrics.00Ce\xf0\x9f\x90\xa7 \xf0\x9f\xae\x96 \xe2\x8a\x86 <code>LinuxTermios</code> output flags.00Dk\xe2\x9c\xa8 \xf0\x9f\x97\x83\xef\xb8\x8f A fully <code>const</code> static hashmap with compile-time \xe2\x80\xa600CjThe channel count is zero or cannot map to current PCM \xe2\x80\xa600BlThe input ended inside a double-quoted word.00BlThe input ended inside a single-quoted word.00Bi<code>506 Variant Also Negotiates</code>.DeLanewise saturating <code>a + b</code> with lanes as <code>i8</code>.0DeLanewise saturating <code>a + b</code> with lanes as <code>u8</code>.0DcAdds the scalar <code>rhs</code> to each lane in <code>self</code>.ClFormats the number into a provided buffer and returns it \xe2\x80\xa60000000000000CoFinds the index of the last 1 in <code>self</code> from the \xe2\x80\xa600ClGets the mask of all bits up to and including the lowest \xe2\x80\xa60CoUnsets the bits in <code>self</code> to 0, from the checked \xe2\x80\xa600DfZero out all high bits in a <code>u32</code> starting at the index \xe2\x80\xa6DfZero out all high bits in a <code>u64</code> starting at the index \xe2\x80\xa6CnShifts all bits in the entire register left by a number of \xe2\x80\xa6CmShifts each <code>u128</code> lane left by a number of bytes.ClShifts all bits in the entire register right by a number \xe2\x80\xa6CnShifts each <code>u128</code> lane right by a number of bytes.D`Bit-preserving cast to <code>m128</code> from <code>m128d</code>D`Bit-preserving cast to <code>m128</code> from <code>m128i</code>D`Bit-preserving cast to <code>m128d</code> from <code>m128</code>D`Bit-preserving cast to <code>m128i</code> from <code>m128</code>DaBit-preserving cast to <code>m256</code> from <code>m256d</code>.DaBit-preserving cast to <code>m256</code> from <code>m256i</code>.DaBit-preserving cast to <code>m256i</code> from <code>m256</code>.0DlLow lane <code>(!a.is_nan()) &amp;amp; (!b.is_nan())</code>, other lanes \xe2\x80\xa6EaReturns <code>true</code> if all bits in <code>CLEAR_ON_ENTER</code> are present.EaReturns <code>true</code> if all bits in <code>GENERATOR_WEAK</code> are present.DgDivides each lane by the scalar <code>rhs</code> (truncating division).CeCreate a new zero-sized slice in the provided buffer.EhGets an <code>i8</code> value out of an <code>m256i</code>, returns as <code>i32</code>.CkPrimitive-index variant of <code>from_array_clamped</code>.Bg<em>const</em> method equivalent to \xe2\x80\xa6ClCreates a new buffer by moving all the possible elements \xe2\x80\xa60DlConverts a <code>[u8; N]</code> into <code>[char7; N]</code> without checks.ChConverts an exclusive slice of bytes to an exclusive \xe2\x80\xa6BhLanewise fused <code>-(a * b) + c</code>0BhLanewise fused <code>-(a * b) - c</code>0DhReads from indices in <code>slice</code> to construct a SIMD vector. \xe2\x80\xa6DdGets the value of the bits in <code>self</code> from the checked \xe2\x80\xa600000DiReturns <code>true</code> if <code>RESET_STYLE_ON_DROP</code> is present.EhCreates a mismatch where <code>need</code> is an <code>Interval::closed_open</code>, \xe2\x80\xa6EhCreates a mismatch where <code>need</code> is an <code>Interval::empty_const</code>, \xe2\x80\xa6ClInsert a key-value pair into the map without checking if \xe2\x80\xa6ChInsert a value the set without checking if the value \xe2\x80\xa6EcReturns <code>true</code> if the set shares any bit with <code>ENTROPY_WEAK</code>.EcReturns <code>true</code> if the set shares any bit with <code>FOCUS_EVENTS</code>.EcReturns <code>true</code> if the set shares any bit with <code>MOUSE_MOTION</code>.EcReturns <code>true</code> if the set shares any bit with <code>PROPORTIONAL</code>.EcReturns <code>true</code> if the set shares any bit with <code>REPRODUCIBLE</code>.EcReturns <code>true</code> if the set shares any bit with <code>SA_NOCLDSTOP</code>.EcReturns <code>true</code> if the set shares any bit with <code>SA_NOCLDWAIT</code>.EcReturns <code>true</code> if the set shares any bit with <code>SA_RESETHAND</code>.DjConsumes the <code>Box</code>, returning a wrapped raw pointer and the \xe2\x80\xa6DnConsumes the <code>Weak&lt;T&gt;</code>, returning the wrapped pointer and \xe2\x80\xa6DcConsumes the <code>Rc</code>, returning the wrapped pointer and \xe2\x80\xa6CeReturns <code>true</code> if the field value is zero.D`Returns <code>true</code> if the field value equals its maximum.DhWhether <code>self</code> is equal to <code>NUM_MAX_POWER_OF_TWO</code>.0001CnIterator over generated IDs with <code>AcqRel</code> ordering.CkLoads the reference into a register, replacing the high \xe2\x80\xa6EhComputes the non-negative modulo of <code>self</code> over <code>modulus</code>, and \xe2\x80\xa6EfComputes the non-negative modulo of <code>self</code> over |<code>modulus</code>|, \xe2\x80\xa61010101010DgReturns the middle exclusive sub-<code>string</code> with the given \xe2\x80\xa6EeLanewise <code>a * b</code> with lanes as <code>i16</code>, keep the high bits of \xe2\x80\xa6DfMultiply the <code>i16</code> lanes and keep the high half of each \xe2\x80\xa6DePerforms a \xe2\x80\x9ccarryless\xe2\x80\x9d multiplication of two <code>i64</code> \xe2\x80\xa6CdMultiplies each lane by the scalar <code>rhs</code>.EeLanewise <code>a * b</code> with lanes as <code>u16</code>, keep the high bits of \xe2\x80\xa6DfMultiply the <code>u16</code> lanes and keep the high half of each \xe2\x80\xa6EfMultiplies the odd <code>i32</code> lanes and gives the widened (<code>i64</code>) \xe2\x80\xa6EfMultiplies the odd <code>u32</code> lanes and gives the widened (<code>u64</code>) \xe2\x80\xa6CeReturns the offset of the stored data as a usize, \xe2\x80\xa60Ce(eventCounts) Retrieves the count of recorded events.Cg(timeOrigin) Retrieves the time origin in milliseconds.ChProbes terminal protocol capabilities exposed by the \xe2\x80\xa6B`Returns metadata for this field.DnReturns a subslice from <code>start</code> (inclusive) to <code>end</code> \xe2\x80\xa6DoReturns a substring from <code>start</code> (inclusive) to <code>end</code> \xe2\x80\xa6BnApplies scalar modular reduction to each lane.CmReturns the number of additional elements that fit within \xe2\x80\xa60000CnReturns the additional elements that can be pushed without \xe2\x80\xa611Ch(requestAnimationFrame) Requests an animation frame, \xe2\x80\xa6CgConverts the current stack to a different capacity, \xe2\x80\xa6ClConverts the current stack to a different capacity while \xe2\x80\xa6DlCasts <code>self</code> to <code>i128</code> clamping at the numeric bounds.00E`Casts from <code>u16</code> to <code>i128</code> clamping at the numeric bounds.EaCasts from <code>u128</code> to <code>i128</code> clamping at the numeric bounds.E`Casts from <code>u32</code> to <code>i128</code> clamping at the numeric bounds.E`Casts from <code>i16</code> to <code>i128</code> clamping at the numeric bounds.E`Casts from <code>i32</code> to <code>i128</code> clamping at the numeric bounds.E`Casts from <code>u64</code> to <code>i128</code> clamping at the numeric bounds.EbCasts from <code>isize</code> to <code>i128</code> clamping at the numeric bounds.DoCasts from <code>i8</code> to <code>i128</code> clamping at the numeric bounds.E`Casts from <code>i64</code> to <code>i128</code> clamping at the numeric bounds.DoCasts from <code>u8</code> to <code>i128</code> clamping at the numeric bounds.EaCasts from <code>i128</code> to <code>i128</code> clamping at the numeric bounds.EbCasts from <code>usize</code> to <code>i128</code> clamping at the numeric bounds.DlCasts <code>self</code> to <code>u128</code> clamping at the numeric bounds.00EaCasts from <code>i128</code> to <code>u128</code> clamping at the numeric bounds.DoCasts from <code>i8</code> to <code>u128</code> clamping at the numeric bounds.E`Casts from <code>i32</code> to <code>u128</code> clamping at the numeric bounds.E`Casts from <code>u32</code> to <code>u128</code> clamping at the numeric bounds.EbCasts from <code>usize</code> to <code>u128</code> clamping at the numeric bounds.DoCasts from <code>u8</code> to <code>u128</code> clamping at the numeric bounds.E`Casts from <code>i64</code> to <code>u128</code> clamping at the numeric bounds.EaCasts from <code>u128</code> to <code>u128</code> clamping at the numeric bounds.EbCasts from <code>isize</code> to <code>u128</code> clamping at the numeric bounds.E`Casts from <code>u64</code> to <code>u128</code> clamping at the numeric bounds.E`Casts from <code>i16</code> to <code>u128</code> clamping at the numeric bounds.E`Casts from <code>u16</code> to <code>u128</code> clamping at the numeric bounds.DeSets <code>MOUSE_SGR_PIXELS</code> if <code>condition</code> is true.DeSets <code>PROCESSED_OUTPUT</code> if <code>condition</code> is true.BfSets <code>RESET_STYLE_ON_DROP</code>.EdSets the lowest lane of an <code>m128d</code> as all lanes of an <code>m256d</code>.DeSets <code>UNDERLINE_DOUBLE</code> if <code>condition</code> is true.DdSets the given <code>value</code> into the bits from the checked \xe2\x80\xa600000FbShuffle <code>f32</code> values in <code>a</code> using <code>i32</code> values in <code>v</code>.FaShuffle <code>f32</code> lanes in <code>a</code> using <code>i32</code> values in <code>v</code>.ChSplat the lowest 16-bit lane across the entire 128 bits.ChSplat the lowest 32-bit lane across the entire 128 bits.ChSplat the lowest 64-bit lane across the entire 128 bits.DeLanewise saturating <code>a - b</code> with lanes as <code>i8</code>.0DeLanewise saturating <code>a - b</code> with lanes as <code>u8</code>.0DjSubtracts the scalar <code>rhs</code> from each lane in <code>self</code>.D`Returns the last <code>n</code> elements of the exclusive slice.ClConverts a character to its closest ASCII equivalent, or \xe2\x80\xa6CmConverts the Unicode scalar value to a UTF-8 encoded byte \xe2\x80\xa6CnCounts the number of trailing zero bits in a <code>u32</code>.CnCounts the number of trailing zero bits in a <code>u64</code>.DkReturns a subslice without the given leading and edges <code>byte</code>\xe2\x80\xa6FcTruncate the <code>f64</code> lanes to the lower <code>i32</code> lanes (upper <code>i32</code> \xe2\x80\xa6CaTruncate the lower lane into an <code>i32</code>.CaTruncate the lower lane into an <code>i64</code>.ClConstructs a new boxed slice with uninitialized contents \xe2\x80\xa60BeReplaces the field value, if it fits.FeReturns <code>self</code> with <code>BRACKETED_PASTE</code> inserted if <code>condition</code> is \xe2\x80\xa6CgConverts an address back to a pointer, picking up a \xe2\x80\xa600000CmConverts an address back to a mutable pointer, picking up \xe2\x80\xa6CjConverts an address back to a pointer, picking up some \xe2\x80\xa6DjReturns <code>self</code> with the field value replaced, masking it to \xe2\x80\xa6DdReturns <code>self</code> with <code>BRACKETED_PASTE</code> removed.EfWrites this Unicode scalar as UTF-8 into <code>buf</code> <strong>without </strong>\xe2\x80\xa6fU+000900Di\xe2\x9c\xa8 \xf0\x9f\x97\x83\xef\xb8\x8f An owned linear buffer over a <code>Vec</code>, made with \xe2\x80\xa600CjCode to disable SGR extended mouse encoding with pixel \xe2\x80\xa6fU+001C00Ck\xf0\x9f\x94\x84 \xf0\x9f\x8d\x83 A lending iterator with a known remaining length.00Cf\xf0\x9f\x90\xa7 \xf0\x9f\xae\x96 \xe2\x8a\x86 <code>LinuxTermios</code> control flags.00CiQueries the state/support of a DEC private mode. (DECRQM)CmThe bits-per-sample field is not supported by current PCM \xe2\x80\xa600EgAdd horizontal pairs of <code>i16</code> values, pack the outputs as <code>a</code> \xe2\x80\xa6CmHorizontal <code>a + b</code> with lanes as <code>i16</code>.EgAdd horizontal pairs of <code>i32</code> values, pack the outputs as <code>a</code> \xe2\x80\xa6CmHorizontal <code>a + b</code> with lanes as <code>i32</code>.DfLanewise saturating <code>a + b</code> with lanes as <code>i16</code>.0DfLanewise saturating <code>a + b</code> with lanes as <code>u16</code>.0BfAssist in expanding an AES cipher key.D`Finds the index of the first 1 in <code>self</code> from the \xe2\x80\xa600CoFinds the index of the last 0 in <code>self</code> from the \xe2\x80\xa600EhSets the bits in <code>self</code> to 1, from the checked <code>[start..=end]</code> \xe2\x80\xa600BcResets (clears) the lowest set bit.0CkGets the value of the lowest set bit in a <code>u32</code>.CkGets the value of the lowest set bit in a <code>u64</code>.DaBit-preserving cast to <code>m128d</code> from <code>m128i</code>DbBit-preserving cast to <code>m128d</code> from <code>m256d</code>.DaBit-preserving cast to <code>m128i</code> from <code>m128d</code>DbBit-preserving cast to <code>m128i</code> from <code>m256i</code>.DbBit-preserving cast to <code>m256d</code> from <code>m256i</code>.DbBit-preserving cast to <code>m256i</code> from <code>m256d</code>.DbCasts <code>self</code> to <code>isize_up</code> with range check.00DgCasts from <code>i128</code> to <code>isize_up</code> with range check.DfCasts from <code>u64</code> to <code>isize_up</code> with range check.DfCasts from <code>i16</code> to <code>isize_up</code> with range check.DeCasts from <code>u8</code> to <code>isize_up</code> with range check.DgCasts from <code>u128</code> to <code>isize_up</code> with range check.DfCasts from <code>u32</code> to <code>isize_up</code> with range check.DfCasts from <code>i32</code> to <code>isize_up</code> with range check.DeCasts from <code>i8</code> to <code>isize_up</code> with range check.DfCasts from <code>u16</code> to <code>isize_up</code> with range check.DhCasts from <code>usize</code> to <code>isize_up</code> with range check.DfCasts from <code>i64</code> to <code>isize_up</code> with range check.DhCasts from <code>isize</code> to <code>isize_up</code> with range check.DbCasts <code>self</code> to <code>usize_up</code> with range check.00DfCasts from <code>i64</code> to <code>usize_up</code> with range check.DgCasts from <code>u128</code> to <code>usize_up</code> with range check.DeCasts from <code>u8</code> to <code>usize_up</code> with range check.DfCasts from <code>u64</code> to <code>usize_up</code> with range check.DfCasts from <code>u16</code> to <code>usize_up</code> with range check.DfCasts from <code>u32</code> to <code>usize_up</code> with range check.DhCasts from <code>usize</code> to <code>usize_up</code> with range check.DgCasts from <code>i128</code> to <code>usize_up</code> with range check.DfCasts from <code>i32</code> to <code>usize_up</code> with range check.DeCasts from <code>i8</code> to <code>usize_up</code> with range check.DhCasts from <code>isize</code> to <code>usize_up</code> with range check.DfCasts from <code>i16</code> to <code>usize_up</code> with range check.AgClears the field value.DkLow lane <code>(!a.is_nan()) &amp;amp; (!b.is_nan())</code>, other lane \xe2\x80\xa6EbReturns <code>true</code> if all bits in <code>BRACKETED_PASTE</code> are present.FjCopies <code>count * size_of::&lt;T&gt;()</code> bytes from <code>src</code> to <code>self</code>. The \xe2\x80\xa6DaCounts the number of 1s in <code>self</code> from the checked \xe2\x80\xa600000ClDecodes a basic quoted string payload into <code>out</code>.CgReturns the ASCII decimal digit at the specified index.00000DkReturns <code>Some(ASCII digit)</code> if the index is within the number\xe2\x80\xa600000CkComputes the minimal number of digits required so that, \xe2\x80\xa6BjDuplicate the odd lanes to the even lanes.CbDuplicate the odd-indexed lanes to the even lanes.Cd(addEventListener) Attaches a Rust function as a \xe2\x80\xa60Dj(removeEventListener) Removes a JavaScript function <code>event</code> \xe2\x80\xa6EiGets an <code>i16</code> value out of an <code>m128i</code>, returns as <code>i32</code>.EiGets an <code>i16</code> value out of an <code>m256i</code>, returns as <code>i32</code>.CfExtracts an <code>m128d</code> from <code>m256d</code>CfExtracts an <code>m128i</code> from <code>m256i</code>CjWrites the prime factors in the given <code>buffer</code>.00000000000CcReturns the unique prime factors with its exponent.00000000000DaConverts a timed normalized <code>EventPointer</code> back to \xe2\x80\xa6Bg<em>const</em> method equivalent to \xe2\x80\xa600000CjLow lane <code>-(a * b) + c</code>, other lanes unchanged.D`Low lane fused <code>-(a * b) - c</code>, other lanes unchanged.CkCalculates the 1D array index from the given 2D coordinates0BaReturns the unique prime factors.000BhCalls `NumInt::int_ref_digits_base_sign.CdSimilar to int_digits_base_sign, but operates on \xe2\x80\xa6101100BhCalls `NumInt::int_ref_div_ties_towards.CdSimilar to int_div_ties_towards, but operates on \xe2\x80\xa6101100EdReturns <code>true</code> if the set shares any bit with <code>CLEAR_ON_DROP</code>.EdReturns <code>true</code> if the set shares any bit with <code>CRYPTOGRAPHIC</code>.EdReturns <code>true</code> if the set shares any bit with <code>DETERMINISTIC</code>.CeReturns <code>true</code> if the field value is zero.0EcReturns <code>true</code> if <code>self</code> has a negative sign and is not zero.0000EcReturns <code>true</code> if <code>self</code> has a positive sign and is not zero.0000CkCalculates the modular additive inverse, unchecked version.00000000000ClCalculates the modular multiplicative inverse, unchecked \xe2\x80\xa600000000000DgReturns the middle exclusive sub-<code>string</code> with the given \xe2\x80\xa6DkConsumes and returns the next line before <code>byte</code>, trimmed of \xe2\x80\xa6DkReturns an exclusive subslice starting from the given <code>start</code>\xe2\x80\xa6CkReads the CPU\xe2\x80\x99s timestamp counter value and store the \xe2\x80\xa6CgRefreshes runtime capabilities from cached terminal \xe2\x80\xa6CeRefreshes and returns the currently known service \xe2\x80\xa600DmCasts <code>self</code> to <code>isize</code> clamping at the numeric bounds.00EaCasts from <code>u16</code> to <code>isize</code> clamping at the numeric bounds.EcCasts from <code>usize</code> to <code>isize</code> clamping at the numeric bounds.EbCasts from <code>i128</code> to <code>isize</code> clamping at the numeric bounds.EaCasts from <code>i64</code> to <code>isize</code> clamping at the numeric bounds.EbCasts from <code>u128</code> to <code>isize</code> clamping at the numeric bounds.EaCasts from <code>u64</code> to <code>isize</code> clamping at the numeric bounds.E`Casts from <code>u8</code> to <code>isize</code> clamping at the numeric bounds.EaCasts from <code>u32</code> to <code>isize</code> clamping at the numeric bounds.EaCasts from <code>i32</code> to <code>isize</code> clamping at the numeric bounds.EaCasts from <code>i16</code> to <code>isize</code> clamping at the numeric bounds.EcCasts from <code>isize</code> to <code>isize</code> clamping at the numeric bounds.E`Casts from <code>i8</code> to <code>isize</code> clamping at the numeric bounds.DmCasts <code>self</code> to <code>usize</code> clamping at the numeric bounds.00EcCasts from <code>usize</code> to <code>usize</code> clamping at the numeric bounds.EbCasts from <code>u128</code> to <code>usize</code> clamping at the numeric bounds.EaCasts from <code>i32</code> to <code>usize</code> clamping at the numeric bounds.EaCasts from <code>i64</code> to <code>usize</code> clamping at the numeric bounds.E`Casts from <code>i8</code> to <code>usize</code> clamping at the numeric bounds.")