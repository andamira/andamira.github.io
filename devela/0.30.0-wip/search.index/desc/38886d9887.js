rd_("EeLanewise <code>a * b</code> with lanes as <code>i16</code>, keep the high bits of \xe2\x80\xa6DfMultiply the <code>i16</code> lanes and keep the high half of each \xe2\x80\xa6DePerforms a \xe2\x80\x9ccarryless\xe2\x80\x9d multiplication of two <code>i64</code> \xe2\x80\xa6CdMultiplies each lane by the scalar <code>rhs</code>.EeLanewise <code>a * b</code> with lanes as <code>u16</code>, keep the high bits of \xe2\x80\xa6DfMultiply the <code>u16</code> lanes and keep the high half of each \xe2\x80\xa6EfMultiplies the odd <code>i32</code> lanes and gives the widened (<code>i64</code>) \xe2\x80\xa6EfMultiplies the odd <code>u32</code> lanes and gives the widened (<code>u64</code>) \xe2\x80\xa6Ce(eventCounts) Retrieves the count of recorded events.Cg(timeOrigin) Retrieves the time origin in milliseconds.BfProbes terminal protocol capabilities.0ChProbes terminal protocol capabilities exposed by the \xe2\x80\xa611B`Returns metadata for this field.DnReturns a subslice from <code>start</code> (inclusive) to <code>end</code> \xe2\x80\xa6DoReturns a substring from <code>start</code> (inclusive) to <code>end</code> \xe2\x80\xa6DkReturns a reduced copy with a positive denominator, or <code>self</code>\xe2\x80\xa600000000000BnApplies scalar modular reduction to each lane.CmReturns the number of additional elements that fit within \xe2\x80\xa6000CnReturns the additional elements that can be pushed without \xe2\x80\xa611111Ch(requestAnimationFrame) Requests an animation frame, \xe2\x80\xa6DiAdds <code>rhs</code>, preserving metadata, saturating to the payload \xe2\x80\xa60DfSubtracts <code>rhs</code>, preserving metadata, saturating to the \xe2\x80\xa60DlCasts <code>self</code> to <code>i128</code> clamping at the numeric bounds.EbCasts from <code>usize</code> to <code>i128</code> clamping at the numeric bounds.DoCasts from <code>i8</code> to <code>i128</code> clamping at the numeric bounds.E`Casts from <code>u32</code> to <code>i128</code> clamping at the numeric bounds.DoCasts from <code>u8</code> to <code>i128</code> clamping at the numeric bounds.E`Casts from <code>i64</code> to <code>i128</code> clamping at the numeric bounds.E`Casts from <code>i16</code> to <code>i128</code> clamping at the numeric bounds.EaCasts from <code>i128</code> to <code>i128</code> clamping at the numeric bounds.E`Casts from <code>u16</code> to <code>i128</code> clamping at the numeric bounds.E`Casts from <code>i32</code> to <code>i128</code> clamping at the numeric bounds.EbCasts from <code>isize</code> to <code>i128</code> clamping at the numeric bounds.E`Casts from <code>u64</code> to <code>i128</code> clamping at the numeric bounds.EaCasts from <code>u128</code> to <code>i128</code> clamping at the numeric bounds.<<DlCasts <code>self</code> to <code>u128</code> clamping at the numeric bounds.EaCasts from <code>u128</code> to <code>u128</code> clamping at the numeric bounds.DoCasts from <code>i8</code> to <code>u128</code> clamping at the numeric bounds.E`Casts from <code>u16</code> to <code>u128</code> clamping at the numeric bounds.E`Casts from <code>u32</code> to <code>u128</code> clamping at the numeric bounds.E`Casts from <code>i32</code> to <code>u128</code> clamping at the numeric bounds.EbCasts from <code>usize</code> to <code>u128</code> clamping at the numeric bounds.DoCasts from <code>u8</code> to <code>u128</code> clamping at the numeric bounds.EaCasts from <code>i128</code> to <code>u128</code> clamping at the numeric bounds.E`Casts from <code>i64</code> to <code>u128</code> clamping at the numeric bounds.E`Casts from <code>i16</code> to <code>u128</code> clamping at the numeric bounds.E`Casts from <code>u64</code> to <code>u128</code> clamping at the numeric bounds.EbCasts from <code>isize</code> to <code>u128</code> clamping at the numeric bounds.<<DeSets <code>MOUSE_SGR_PIXELS</code> if <code>condition</code> is true.DeSets <code>PROCESSED_OUTPUT</code> if <code>condition</code> is true.BfSets <code>RESET_STYLE_ON_DROP</code>.EdSets the lowest lane of an <code>m128d</code> as all lanes of an <code>m256d</code>.DeSets <code>UNDERLINE_DOUBLE</code> if <code>condition</code> is true.DdSets the given <code>value</code> into the bits from the checked \xe2\x80\xa600000FbShuffle <code>f32</code> values in <code>a</code> using <code>i32</code> values in <code>v</code>.FaShuffle <code>f32</code> lanes in <code>a</code> using <code>i32</code> values in <code>v</code>.ChSplat the lowest 16-bit lane across the entire 128 bits.ChSplat the lowest 32-bit lane across the entire 128 bits.ChSplat the lowest 64-bit lane across the entire 128 bits.DeLanewise saturating <code>a - b</code> with lanes as <code>i8</code>.0DeLanewise saturating <code>a - b</code> with lanes as <code>u8</code>.0DjSubtracts the scalar <code>rhs</code> from each lane in <code>self</code>.D`Returns the last <code>n</code> elements of the exclusive slice.ClConverts a character to its closest ASCII equivalent, or \xe2\x80\xa6CmConverts the Unicode scalar value to a UTF-8 encoded byte \xe2\x80\xa6CnCounts the number of trailing zero bits in a <code>u32</code>.CnCounts the number of trailing zero bits in a <code>u64</code>.DkReturns a subslice without the given leading and edges <code>byte</code>\xe2\x80\xa6FcTruncate the <code>f64</code> lanes to the lower <code>i32</code> lanes (upper <code>i32</code> \xe2\x80\xa6CaTruncate the lower lane into an <code>i32</code>.CaTruncate the lower lane into an <code>i64</code>.ClConstructs a new boxed slice with uninitialized contents \xe2\x80\xa60BeReplaces the field value, if it fits.BfUnsets <code>PersistentStorage</code>.FeReturns <code>self</code> with <code>BRACKETED_PASTE</code> inserted if <code>condition</code> is \xe2\x80\xa6CmConverts an address back to a mutable pointer, picking up \xe2\x80\xa6CjConverts an address back to a pointer, picking up some \xe2\x80\xa6DjReturns <code>self</code> with the field value replaced, masking it to \xe2\x80\xa6DdReturns <code>self</code> with <code>BRACKETED_PASTE</code> removed.EfWrites this Unicode scalar as UTF-8 into <code>buf</code> <strong>without </strong>\xe2\x80\xa6Cn\xe2\x9c\xa8 \xf0\x9f\xab\x86 An index handle into <code>ArenaStringExample</code>.00fU+000900Di\xe2\x9c\xa8 \xf0\x9f\x97\x83\xef\xb8\x8f An owned linear buffer over a <code>Vec</code>, made with \xe2\x80\xa600CjCode to disable SGR extended mouse encoding with pixel \xe2\x80\xa6Dc\xe2\x9c\xa8 \xf0\x9f\xab\x86 \xe2\x9e\xb0 An edge handle into <code>GraphAdjAllocExample</code>.00Dc\xe2\x9c\xa8 \xf0\x9f\xab\x86 \xe2\x9e\xb0 An edge handle into <code>GraphCsrAllocExample</code>.00fU+001C00Ch\xe2\x9c\xa8 \xf0\x9f\x97\x83\xef\xb8\x8f An example allocating UTF-8 string interner.00Ck\xf0\x9f\x94\x84 \xf0\x9f\x8d\x83 A lending iterator with a known remaining length.00Cf\xf0\x9f\x90\xa7 \xf0\x9f\xae\x96 \xe2\x8a\x86 <code>LinuxTermios</code> control flags.00CiQueries the state/support of a DEC private mode. (DECRQM)CmThe bits-per-sample field is not supported by current PCM \xe2\x80\xa600EgAdd horizontal pairs of <code>i16</code> values, pack the outputs as <code>a</code> \xe2\x80\xa6CmHorizontal <code>a + b</code> with lanes as <code>i16</code>.EgAdd horizontal pairs of <code>i32</code> values, pack the outputs as <code>a</code> \xe2\x80\xa6CmHorizontal <code>a + b</code> with lanes as <code>i32</code>.DfLanewise saturating <code>a + b</code> with lanes as <code>i16</code>.0DfLanewise saturating <code>a + b</code> with lanes as <code>u16</code>.0BfAssist in expanding an AES cipher key.D`Finds the index of the first 1 in <code>self</code> from the \xe2\x80\xa600CoFinds the index of the last 0 in <code>self</code> from the \xe2\x80\xa600EhSets the bits in <code>self</code> to 1, from the checked <code>[start..=end]</code> \xe2\x80\xa600BcResets (clears) the lowest set bit.0CkGets the value of the lowest set bit in a <code>u32</code>.CkGets the value of the lowest set bit in a <code>u64</code>.DaBit-preserving cast to <code>m128d</code> from <code>m128i</code>DbBit-preserving cast to <code>m128d</code> from <code>m256d</code>.DaBit-preserving cast to <code>m128i</code> from <code>m128d</code>DbBit-preserving cast to <code>m128i</code> from <code>m256i</code>.DbBit-preserving cast to <code>m256d</code> from <code>m256i</code>.DbBit-preserving cast to <code>m256i</code> from <code>m256d</code>.DbCasts <code>self</code> to <code>isize_up</code> with range check.DhCasts from <code>usize</code> to <code>isize_up</code> with range check.DhCasts from <code>isize</code> to <code>isize_up</code> with range check.DfCasts from <code>u16</code> to <code>isize_up</code> with range check.DfCasts from <code>u64</code> to <code>isize_up</code> with range check.DfCasts from <code>u32</code> to <code>isize_up</code> with range check.DfCasts from <code>i64</code> to <code>isize_up</code> with range check.DeCasts from <code>u8</code> to <code>isize_up</code> with range check.DeCasts from <code>i8</code> to <code>isize_up</code> with range check.DgCasts from <code>i128</code> to <code>isize_up</code> with range check.DgCasts from <code>u128</code> to <code>isize_up</code> with range check.DfCasts from <code>i16</code> to <code>isize_up</code> with range check.DfCasts from <code>i32</code> to <code>isize_up</code> with range check.<<DbCasts <code>self</code> to <code>usize_up</code> with range check.DgCasts from <code>i128</code> to <code>usize_up</code> with range check.DeCasts from <code>i8</code> to <code>usize_up</code> with range check.DgCasts from <code>u128</code> to <code>usize_up</code> with range check.DfCasts from <code>u16</code> to <code>usize_up</code> with range check.DfCasts from <code>i32</code> to <code>usize_up</code> with range check.DfCasts from <code>u64</code> to <code>usize_up</code> with range check.DeCasts from <code>u8</code> to <code>usize_up</code> with range check.DhCasts from <code>usize</code> to <code>usize_up</code> with range check.DhCasts from <code>isize</code> to <code>usize_up</code> with range check.DfCasts from <code>i16</code> to <code>usize_up</code> with range check.DfCasts from <code>u32</code> to <code>usize_up</code> with range check.DfCasts from <code>i64</code> to <code>usize_up</code> with range check.<<AgClears the field value.DkLow lane <code>(!a.is_nan()) &amp;amp; (!b.is_nan())</code>, other lane \xe2\x80\xa6EbReturns <code>true</code> if all bits in <code>BRACKETED_PASTE</code> are present.FjCopies <code>count * size_of::&lt;T&gt;()</code> bytes from <code>src</code> to <code>self</code>. The \xe2\x80\xa6DaCounts the number of 1s in <code>self</code> from the checked \xe2\x80\xa600000ClDecodes a basic quoted string payload into <code>out</code>.CgReturns the ASCII decimal digit at the specified index.00000DkReturns <code>Some(ASCII digit)</code> if the index is within the number\xe2\x80\xa600000CkComputes the minimal number of digits required so that, \xe2\x80\xa6BjDuplicate the odd lanes to the even lanes.CbDuplicate the odd-indexed lanes to the even lanes.Cd(addEventListener) Attaches a Rust function as a \xe2\x80\xa60Dj(removeEventListener) Removes a JavaScript function <code>event</code> \xe2\x80\xa6EiGets an <code>i16</code> value out of an <code>m128i</code>, returns as <code>i32</code>.EiGets an <code>i16</code> value out of an <code>m256i</code>, returns as <code>i32</code>.CfExtracts an <code>m128d</code> from <code>m256d</code>CfExtracts an <code>m128i</code> from <code>m256i</code>CjWrites the prime factors in the given <code>buffer</code>.00000000000CcReturns the unique prime factors with its exponent.00000000000EhConverts a timed normalized <code>EventPointer</code> to <code>WebEventPointer</code>\xe2\x80\xa6Bg<em>const</em> method equivalent to \xe2\x80\xa600000CjLow lane <code>-(a * b) + c</code>, other lanes unchanged.D`Low lane fused <code>-(a * b) - c</code>, other lanes unchanged.BaReturns the unique prime factors.000BhCalls `NumInt::int_ref_digits_base_sign.0CdSimilar to int_digits_base_sign, but operates on \xe2\x80\xa601010BhCalls `NumInt::int_ref_div_ties_towards.0CdSimilar to int_div_ties_towards, but operates on \xe2\x80\xa601010EdReturns <code>true</code> if the set shares any bit with <code>Accelerometer</code>.EdReturns <code>true</code> if the set shares any bit with <code>CLEAR_ON_DROP</code>.EdReturns <code>true</code> if the set shares any bit with <code>ClipboardRead</code>.EdReturns <code>true</code> if the set shares any bit with <code>CRYPTOGRAPHIC</code>.EdReturns <code>true</code> if the set shares any bit with <code>DETERMINISTIC</code>.EdReturns <code>true</code> if the set shares any bit with <code>Notifications</code>.EdReturns <code>true</code> if the set shares any bit with <code>StorageAccess</code>.DgApplies the inverse ratio to <code>value</code>, rounding down to a \xe2\x80\xa600000000000000000000000DdApplies the inverse ratio to <code>value</code>, rounding to the \xe2\x80\xa600000000000000000000000CeReturns <code>true</code> if the field value is zero.0EcReturns <code>true</code> if <code>self</code> has a negative sign and is not zero.0000EcReturns <code>true</code> if <code>self</code> has a positive sign and is not zero.0000CkCalculates the modular additive inverse, unchecked version.00000000000ClCalculates the modular multiplicative inverse, unchecked \xe2\x80\xa600000000000DgReturns the middle exclusive sub-<code>string</code> with the given \xe2\x80\xa6DkConsumes and returns the next line before <code>byte</code>, trimmed of \xe2\x80\xa6DkReturns an exclusive subslice starting from the given <code>start</code>\xe2\x80\xa6CkReads the CPU\xe2\x80\x99s timestamp counter value and store the \xe2\x80\xa6CgRefreshes runtime capabilities from cached terminal \xe2\x80\xa6CeRefreshes and returns the currently known service \xe2\x80\xa600CkReturns the exact duration of one sample period in seconds.DmCasts <code>self</code> to <code>isize</code> clamping at the numeric bounds.EcCasts from <code>isize</code> to <code>isize</code> clamping at the numeric bounds.E`Casts from <code>u8</code> to <code>isize</code> clamping at the numeric bounds.EaCasts from <code>i64</code> to <code>isize</code> clamping at the numeric bounds.EaCasts from <code>i32</code> to <code>isize</code> clamping at the numeric bounds.EbCasts from <code>u128</code> to <code>isize</code> clamping at the numeric bounds.EcCasts from <code>usize</code> to <code>isize</code> clamping at the numeric bounds.EaCasts from <code>u32</code> to <code>isize</code> clamping at the numeric bounds.EaCasts from <code>u64</code> to <code>isize</code> clamping at the numeric bounds.EaCasts from <code>i16</code> to <code>isize</code> clamping at the numeric bounds.EbCasts from <code>i128</code> to <code>isize</code> clamping at the numeric bounds.EaCasts from <code>u16</code> to <code>isize</code> clamping at the numeric bounds.E`Casts from <code>i8</code> to <code>isize</code> clamping at the numeric bounds.<<DmCasts <code>self</code> to <code>usize</code> clamping at the numeric bounds.EaCasts from <code>i16</code> to <code>usize</code> clamping at the numeric bounds.EcCasts from <code>isize</code> to <code>usize</code> clamping at the numeric bounds.EaCasts from <code>u32</code> to <code>usize</code> clamping at the numeric bounds.E`Casts from <code>u8</code> to <code>usize</code> clamping at the numeric bounds.EbCasts from <code>u128</code> to <code>usize</code> clamping at the numeric bounds.EaCasts from <code>i32</code> to <code>usize</code> clamping at the numeric bounds.EaCasts from <code>u64</code> to <code>usize</code> clamping at the numeric bounds.EaCasts from <code>u16</code> to <code>usize</code> clamping at the numeric bounds.EbCasts from <code>i128</code> to <code>usize</code> clamping at the numeric bounds.EcCasts from <code>usize</code> to <code>usize</code> clamping at the numeric bounds.EaCasts from <code>i64</code> to <code>usize</code> clamping at the numeric bounds.E`Casts from <code>i8</code> to <code>usize</code> clamping at the numeric bounds.<<ClWrites values from a SIMD vector to multiple potentially \xe2\x80\xa6DfSets <code>PersistentStorage</code> if <code>condition</code> is true.CgSet <code>m128d</code> args into an <code>m256d</code>.CgSet <code>m128i</code> args into an <code>m256i</code>.FaShuffle the <code>f32</code> lanes from <code>$a</code> and <code>$b</code> together using an \xe2\x80\xa6EdShuffle the <code>i32</code> lanes in <code>$a</code> using an immediate control \xe2\x80\xa6EfShuffle the <code>f64</code> lanes from <code>$a</code> using an immediate control \xe2\x80\xa6EdShuffle the <code>f64</code> lanes in <code>$a</code> using an immediate control \xe2\x80\xa6FbShuffle <code>f32</code> values in <code>a</code> using <code>i32</code> values in <code>v</code>.FlShuffle <code>f64</code> lanes in <code>a</code> using bit 1 of the <code>i64</code> lanes in <code>v</code>FaShuffle <code>i32</code> lanes in <code>a</code> using <code>i32</code> values in <code>v</code>.EoShuffle <code>i8</code> lanes in <code>a</code> using <code>i8</code> values in <code>v</code>.CjForms a mutable raw slice from a mutable pointer and a \xe2\x80\xa6DjSubtract horizontal pairs of <code>i16</code> values, pack the outputs \xe2\x80\xa6CmHorizontal <code>a - b</code> with lanes as <code>i16</code>.DjSubtract horizontal pairs of <code>i32</code> values, pack the outputs \xe2\x80\xa6CmHorizontal <code>a - b</code> with lanes as <code>i32</code>.DfLanewise saturating <code>a - b</code> with lanes as <code>i16</code>.0DfLanewise saturating <code>a - b</code> with lanes as <code>u16</code>.0CjCompute \xe2\x80\x9csum of <code>u8</code> absolute differences\xe2\x80\x9d.0ClPrimitive-index variant of <code>swap_remove_default</code>.0DaReturns the first <code>n</code> elements of the exclusive slice.ClReturns the slice omitting the last <code>n</code> elements.DiReturns the forward elapsed value from <code>point</code> to now as a \xe2\x80\xa60000000CgConverts all characters in the array to their ASCII \xe2\x80\xa6CmConverts to a <code>TimeDelta</code>, saturating on overflow.DkReturns a subslice without the given leading and edges <code>byte</code>\xe2\x80\xa6BeReplaces the field value, if it fits.0DjReturns <code>self</code> with the checked field value replaced, if it \xe2\x80\xa6DfCreates an empty <code>HashMap</code> with the specified capacity, \xe2\x80\xa6DhCreates an empty <code>HashSet</code> with the specified capacity, usingFcReturns <code>self</code> with <code>MOUSE_SGR_PIXELS</code> inserted if <code>condition</code> \xe2\x80\xa6FcReturns <code>self</code> with <code>PROCESSED_OUTPUT</code> inserted if <code>condition</code> \xe2\x80\xa6DiReturns <code>self</code> with <code>RESET_STYLE_ON_DROP</code> inserted.DnReturns a new <code>LinuxTimespec</code> with the given <code>duration</code>.FcReturns <code>self</code> with <code>UNDERLINE_DOUBLE</code> inserted if <code>condition</code> \xe2\x80\xa6DeReturns <code>self</code> with <code>MOUSE_SGR_PIXELS</code> removed.DeReturns <code>self</code> with <code>PROCESSED_OUTPUT</code> removed.DeReturns <code>self</code> with <code>UNDERLINE_DOUBLE</code> removed.CfWrite sixel sequence start with full raster attributesCf\xe2\x9c\xa8 \xf0\x9f\x97\x83\xef\xb8\x8f A static linear buffer over contiguous \xe2\x80\xa600CdSpanish key for <code>\xc2\xa1</code> and <code>\xc2\xbf</code>.fU+001D00Dh\xe2\x9c\xa8 \xf0\x9f\xab\x86 A canonical string symbol for <code>InternStringExample</code>.00CfPerform the InvMixColumns transform on <code>a</code>.ClFormats the number into a provided buffer and returns it \xe2\x80\xa60000000000000D`Finds the index of the first 0 in <code>self</code> from the \xe2\x80\xa600DaConditionally flips bits in a range if <code>cond</code> is true.00DhReverses the order of the bits in <code>self</code> from the checked \xe2\x80\xa600CkCalculates the distance between two pointers within the \xe2\x80\xa6CnReturns the smallest power of two greater than or equal to \xe2\x80\xa600000CjReturns an OSC 52 sequence that requests the clipboard \xe2\x80\xa6EcReturns <code>true</code> if all bits in <code>MOUSE_SGR_PIXELS</code> are present.EcReturns <code>true</code> if all bits in <code>PROCESSED_OUTPUT</code> are present.EcReturns <code>true</code> if all bits in <code>UNDERLINE_DOUBLE</code> are present.DaCounts the number of 0s in <code>self</code> from the checked \xe2\x80\xa600000DeDecodes Base64 bytes into <code>output</code>, accepting optional \xe2\x80\xa6DjDecodes Base32 bytes into <code>output</code>, accepting relaxed input \xe2\x80\xa6DiDecrements the strong reference count on the <code>Rc&lt;T&gt;</code> \xe2\x80\xa6BjDuplicate the odd lanes to the even lanes.CbDuplicate the even-indexed lanes to the odd lanes.CbDuplicate the odd-indexed lanes to the even lanes.BgCreates a layout, bypassing all checks.DaConverts <code>bytes</code> to the longest complete UTF-8 prefix.CjLow lane <code>-(a * b) + c</code>, other lanes unchanged.D`Low lane fused <code>-(a * b) - c</code>, other lanes unchanged.DkReturns <code>true</code> if <code>TopLevelStorageAccess</code> is present.DiIncrements the strong reference count on the <code>Rc&lt;T&gt;</code> \xe2\x80\xa6E`Inserts the low 16 bits of an <code>i32</code> value into an <code>m128i</code>.BiCalls `NumInt::int_ref_digital_root_base.0CeSimilar to int_digital_root_base, but operates on \xe2\x80\xa601010BiCalls `NumInt::int_ref_factors_prime_buf.0CeSimilar to int_factors_prime_buf, but operates on \xe2\x80\xa601010BiCalls `NumInt::int_ref_modulo_add_cycles.0CeSimilar to int_modulo_add_cycles, but operates on \xe2\x80\xa601010BiCalls `NumInt::int_ref_modulo_mul_cycles.0CeSimilar to int_modulo_mul_cycles, but operates on \xe2\x80\xa601010BiCalls `NumInt::int_ref_modulo_sub_cycles.0CeSimilar to int_modulo_sub_cycles, but operates on \xe2\x80\xa601010EeReturns <code>true</code> if the set shares any bit with <code>BackgroundSync</code>.EeReturns <code>true</code> if the set shares any bit with <code>CLEAR_ON_ENTER</code>.EeReturns <code>true</code> if the set shares any bit with <code>ClipboardWrite</code>.EeReturns <code>true</code> if the set shares any bit with <code>GENERATOR_WEAK</code>.EeReturns <code>true</code> if the set shares any bit with <code>PaymentHandler</code>.EeReturns <code>true</code> if the set shares any bit with <code>ScreenWakeLock</code>.DcDecomposes a <code>Vec&lt;T&gt;</code> into its raw components: \xe2\x80\xa6ChReturns whether all bytes are printable ASCII or spaces.D`Returns <code>true</code> if the field value equals its maximum.BfLoad data from memory into a register.EfMultiply <code>i16</code> lanes into <code>i32</code> intermediates, keep the high \xe2\x80\xa60CkAttempts to choose one valid item using reservoir sampling.000DnReturns a subslice from <code>start</code> (inclusive) to <code>end</code> \xe2\x80\xa6CnReturns the amount of time elapsed from another instant to \xe2\x80\xa6CjSaturating computation of time elapsed from an earlier \xe2\x80\xa6BhSets <code>TopLevelStorageAccess</code>.FaShuffle the <code>f64</code> lanes from <code>$a</code> and <code>$b</code> together using an \xe2\x80\xa6EcShuffle the <code>i32</code> lanes in <code>a</code> using an immediate control \xe2\x80\xa6FmShuffle <code>f64</code> lanes in <code>a</code> using bit 1 of the <code>i64</code> lanes in <code>v</code>.EoShuffle <code>i8</code> lanes in <code>a</code> using <code>i8</code> values in <code>v</code>.CkConsumes a basic double-quoted string, or the remaining \xe2\x80\xa6DjReturns <code>self</code> with the checked field value replaced, if it \xe2\x80\xa60BhUnsets <code>RESET_STYLE_ON_DROP</code>.FdReturns <code>self</code> with <code>PersistentStorage</code> inserted if <code>condition</code> \xe2\x80\xa6DfReturns <code>self</code> with <code>PersistentStorage</code> removed.EfAdds each lane of <code>rhs</code> to the corresponding lane in <code>self</code>, \xe2\x80\xa6E`Casts <code>self</code> to <code>isize_up</code> wrapping at the numeric bounds.EbCasts from <code>isize</code> to <code>isize_up</code> wrapping at the numeric \xe2\x80\xa6EdCasts from <code>i16</code> to <code>isize_up</code> wrapping at the numeric bounds.EcCasts from <code>u8</code> to <code>isize_up</code> wrapping at the numeric bounds.EbCasts from <code>usize</code> to <code>isize_up</code> wrapping at the numeric \xe2\x80\xa6EeCasts from <code>u128</code> to <code>isize_up</code> wrapping at the numeric bounds.EcCasts from <code>i8</code> to <code>isize_up</code> wrapping at the numeric bounds.EdCasts from <code>u16</code> to <code>isize_up</code> wrapping at the numeric bounds.EdCasts from <code>i64</code> to <code>isize_up</code> wrapping at the numeric bounds.EdCasts from <code>i32</code> to <code>isize_up</code> wrapping at the numeric bounds.EdCasts from <code>u32</code> to <code>isize_up</code> wrapping at the numeric bounds.EdCasts from <code>u64</code> to <code>isize_up</code> wrapping at the numeric bounds.EeCasts from <code>i128</code> to <code>isize_up</code> wrapping at the numeric bounds.<<E`Casts <code>self</code> to <code>usize_up</code> wrapping at the numeric bounds.EeCasts from <code>u128</code> to <code>usize_up</code> wrapping at the numeric bounds.EcCasts from <code>u8</code> to <code>usize_up</code> wrapping at the numeric bounds.EbCasts from <code>usize</code> to <code>usize_up</code> wrapping at the numeric \xe2\x80\xa6EdCasts from <code>i16</code> to <code>usize_up</code> wrapping at the numeric bounds.EdCasts from <code>u32</code> to <code>usize_up</code> wrapping at the numeric bounds.EcCasts from <code>i8</code> to <code>usize_up</code> wrapping at the numeric bounds.EdCasts from <code>i64</code> to <code>usize_up</code> wrapping at the numeric bounds.EdCasts from <code>u16</code> to <code>usize_up</code> wrapping at the numeric bounds.EeCasts from <code>i128</code> to <code>usize_up</code> wrapping at the numeric bounds.EbCasts from <code>isize</code> to <code>usize_up</code> wrapping at the numeric \xe2\x80\xa6EdCasts from <code>i32</code> to <code>usize_up</code> wrapping at the numeric bounds.EdCasts from <code>u64</code> to <code>usize_up</code> wrapping at the numeric bounds.<<DiMultiplies each lane of <code>rhs</code> by the corresponding lane in \xe2\x80\xa6DjSubtracts each lane of <code>rhs</code> from the corresponding lane in \xe2\x80\xa6Bo\xe2\x9c\xa8 \xe2\x97\x8e An example rollback position within \xe2\x80\xa600Dd\xe2\x9c\xa8 \xf0\x9f\xab\x86 \xe2\x9e\xb0 A vertex handle into <code>GraphAdjAllocExample</code>.00Dd\xe2\x9c\xa8 \xf0\x9f\xab\x86 \xe2\x9e\xb0 A vertex handle into <code>GraphCsrAllocExample</code>.00Bl<code>505 HTTP Version Not Supported</code>.Cn\xf0\x9f\x94\x84 \xf0\x9f\x8d\x83 A lending iterator that can yield items from the \xe2\x80\xa600Di\xf0\x9f\x93\x81 \xf0\x9f\xaa\x9f <code>std</code> Wraps a Windows path prefix as well as its \xe2\x80\xa600CoUnsets the bits in <code>self</code> to 0, from the checked \xe2\x80\xa600CeReturns the rounded-up byte count for a bit size, \xe2\x80\xa6DdCasts <code>self</code> to <code>isize_down</code> with range check.DgCasts from <code>i8</code> to <code>isize_down</code> with range check.DhCasts from <code>i32</code> to <code>isize_down</code> with range check.DjCasts from <code>isize</code> to <code>isize_down</code> with range check.DhCasts from <code>u16</code> to <code>isize_down</code> with range check.DiCasts from <code>i128</code> to <code>isize_down</code> with range check.DiCasts from <code>u128</code> to <code>isize_down</code> with range check.DjCasts from <code>usize</code> to <code>isize_down</code> with range check.DhCasts from <code>u32</code> to <code>isize_down</code> with range check.DhCasts from <code>i16</code> to <code>isize_down</code> with range check.DhCasts from <code>i64</code> to <code>isize_down</code> with range check.DhCasts from <code>u64</code> to <code>isize_down</code> with range check.DgCasts from <code>u8</code> to <code>isize_down</code> with range check.<<DdCasts <code>self</code> to <code>usize_down</code> with range check.DhCasts from <code>i64</code> to <code>usize_down</code> with range check.DhCasts from <code>u16</code> to <code>usize_down</code> with range check.DhCasts from <code>i16</code> to <code>usize_down</code> with range check.DjCasts from <code>isize</code> to <code>usize_down</code> with range check.DhCasts from <code>u32</code> to <code>usize_down</code> with range check.DgCasts from <code>i8</code> to <code>usize_down</code> with range check.DiCasts from <code>i128</code> to <code>usize_down</code> with range check.DgCasts from <code>u8</code> to <code>usize_down</code> with range check.DhCasts from <code>u64</code> to <code>usize_down</code> with range check.DiCasts from <code>u128</code> to <code>usize_down</code> with range check.DjCasts from <code>usize</code> to <code>usize_down</code> with range check.DhCasts from <code>i32</code> to <code>usize_down</code> with range check.<<EdReturns <code>true</code> if all bits in <code>PersistentStorage</code> are present.EcConvert <code>i32</code> to <code>f32</code> and replace the low lane of the input.DfConvert the lowest <code>f32</code> lane to a single <code>f32</code>.DlRounds the two <code>f64</code> lanes to the low two <code>f32</code> lanes.ClConvert <code>f64</code> lanes to be <code>f32</code> lanes.ClConvert <code>f32</code> lanes to be <code>f64</code> lanes.EhCopies the <code>a</code> value and replaces the low lane with the low <code>b</code>\xe2\x80\xa6CkCopy the low lane of the input to both lanes of the output.Cd(addEventListener) Attaches a Rust function as a \xe2\x80\xa6BjReturns the count of unique prime factors.00000000000DgReturns a string from an array of <code>bytes</code>, which must be \xe2\x80\xa6Bg<em>const</em> method equivalent to \xe2\x80\xa6DjCreates a <code>charu</code> from the first UTF-8 encoded scalar value \xe2\x80\xa6DjCreates a <code>charu_niche</code> from the first UTF-8 encoded scalar \xe2\x80\xa6DjAttempts to get mutable references to <code>N</code> values in the map \xe2\x80\xa60BjCalls `NumInt::int_ref_factors_proper_buf.0CfSimilar to int_factors_proper_buf, but operates on \xe2\x80\xa601010EhReturns <code>true</code> if the set shares any bit with <code>BRACKETED_PASTE</code>\xe2\x80\xa6CeReturns <code>true</code> if the field value is zero.BfLoad data from memory into a register.0DiPrepares layout symbols from the Unicode scalars of <code>text</code> \xe2\x80\xa6DaAppends all copied elements from <code>src</code>, or none if \xe2\x80\xa6DhAppends all copied from <code>src</code> at the back of the ring, or \xe2\x80\xa610DhReturns a subslice up to and including the given <code>end</code> index.DfReturns a substring up to and including the given <code>end</code> \xe2\x80\xa6DhSets <code>RESET_STYLE_ON_DROP</code> if <code>condition</code> is true.EdSets the lowest <code>i8</code> lane of an <code>m128i</code> as all lanes of an \xe2\x80\xa6BgStore data from a register into memory.CnPrimitive-index variant of <code>swap_remove_copy_with</code>.0DfReturns the exclusive slice omitting the last <code>n</code> elements.DgReturns the exclusive string omitting the last <code>n</code> elements.DhReturns the forward elapsed value from <code>point</code> to now, or \xe2\x80\xa60000000BeReplaces the field value, if it fits.E`Returns a new <code>LinuxTimespec</code> with the given <code>time_delta</code>.DkReturns <code>self</code> with <code>TopLevelStorageAccess</code> inserted.CkWrites a string slice, returning the actually written part.Da\xe2\x9c\xa8 \xe2\x97\x8e A rollback mark for <code>ArenaStringAllocExample</code>.00DdGets the value of the bits in <code>self</code> from the checked \xe2\x80\xa600DdSets the given <code>value</code> into the bits from the checked \xe2\x80\xa600EfCounts <code>$a</code> as the high bytes and <code>$b</code> as the low bytes then \xe2\x80\xa6DgWorks like <code>combined_byte_shr_imm_m128i</code>, but twice as wide.EcConvert <code>i32</code> to <code>f64</code> and replace the low lane of the input.EcConvert <code>i64</code> to <code>f64</code> and replace the low lane of the input.DfConvert the lowest <code>f64</code> lane to a single <code>f64</code>.DfConvert the lowest <code>i32</code> lane to a single <code>i32</code>.DgGets the <code>i8</code> lane requested. Only the lowest 4 bits are \xe2\x80\xa6DgFinds the index of the last 1 in <code>self</code> from the checked \xe2\x80\xa600000DgReturns a string from an array of <code>bytes</code>, which must be \xe2\x80\xa6EhReturns a weekday from its index, from <code>Monday=0</code> to <code>Sunday=6</code>\xe2\x80\xa6DiCreates a <code>charu</code> from the first scalar value present in a \xe2\x80\xa6DjCreates a <code>charu_niche</code> from the first scalar value present \xe2\x80\xa6EhReturns a weekday from its index, from <code>Sunday=0</code> to <code>Monday=6</code>\xe2\x80\xa6DkConverts a Vec&lt;u8&gt; to a <code>CString</code> without checking the \xe2\x80\xa6ClReturns <code>true</code> if the set shares any bit with \xe2\x80\xa600EgComputes the modulo of <code>self + other</code> over <code>modulus</code>, and the \xe2\x80\xa60DgComputes the non-negative modulo of <code>self + other</code> over |\xe2\x80\xa6100110001001110000111EgComputes the modulo of <code>self - other</code> over <code>modulus</code>, and the \xe2\x80\xa6DgComputes the non-negative modulo of <code>self - other</code> over |\xe2\x80\xa61110011000DfGenerates a unique <code>IdSeqU64Example</code> ID using the given \xe2\x80\xa6DbPrepends all copied elements from <code>src</code>, or none if \xe2\x80\xa6000DnReturns a subslice from <code>start</code> (inclusive) to <code>end</code> \xe2\x80\xa6DoReturns a substring from <code>start</code> (inclusive) to <code>end</code> \xe2\x80\xa6BnReturns the stored bits occupied by one pixel.000EfAdds each lane of <code>rhs</code> to the corresponding lane in <code>self</code>, \xe2\x80\xa6E`Casts <code>self</code> to <code>isize_up</code> clamping at the numeric bounds.EeCasts from <code>i128</code> to <code>isize_up</code> clamping at the numeric bounds.EcCasts from <code>u8</code> to <code>isize_up</code> clamping at the numeric bounds.EdCasts from <code>u16</code> to <code>isize_up</code> clamping at the numeric bounds.EcCasts from <code>i8</code> to <code>isize_up</code> clamping at the numeric bounds.EdCasts from <code>u32</code> to <code>isize_up</code> clamping at the numeric bounds.EbCasts from <code>isize</code> to <code>isize_up</code> clamping at the numeric \xe2\x80\xa6EdCasts from <code>i32</code> to <code>isize_up</code> clamping at the numeric bounds.EbCasts from <code>usize</code> to <code>isize_up</code> clamping at the numeric \xe2\x80\xa6EdCasts from <code>i16</code> to <code>isize_up</code> clamping at the numeric bounds.EdCasts from <code>i64</code> to <code>isize_up</code> clamping at the numeric bounds.EeCasts from <code>u128</code> to <code>isize_up</code> clamping at the numeric bounds.EdCasts from <code>u64</code> to <code>isize_up</code> clamping at the numeric bounds.<<E`Casts <code>self</code> to <code>usize_up</code> clamping at the numeric bounds.EbCasts from <code>usize</code> to <code>usize_up</code> clamping at the numeric \xe2\x80\xa6EdCasts from <code>i64</code> to <code>usize_up</code> clamping at the numeric bounds.EdCasts from <code>u16</code> to <code>usize_up</code> clamping at the numeric bounds.EdCasts from <code>u32</code> to <code>usize_up</code> clamping at the numeric bounds.EcCasts from <code>i8</code> to <code>usize_up</code> clamping at the numeric bounds.EdCasts from <code>u64</code> to <code>usize_up</code> clamping at the numeric bounds.EdCasts from <code>i32</code> to <code>usize_up</code> clamping at the numeric bounds.EdCasts from <code>i16</code> to <code>usize_up</code> clamping at the numeric bounds.EcCasts from <code>u8</code> to <code>usize_up</code> clamping at the numeric bounds.EeCasts from <code>i128</code> to <code>usize_up</code> clamping at the numeric bounds.EeCasts from <code>u128</code> to <code>usize_up</code> clamping at the numeric bounds.EbCasts from <code>isize</code> to <code>usize_up</code> clamping at the numeric \xe2\x80\xa6<<DjSubtracts each lane of <code>rhs</code> from the corresponding lane in \xe2\x80\xa6EeSets the lowest <code>i16</code> lane of an <code>m128i</code> as all lanes of an \xe2\x80\xa6EeSets the lowest <code>i32</code> lane of an <code>m128i</code> as all lanes of an \xe2\x80\xa6EeSets the lowest <code>i64</code> lane of an <code>m128i</code> as all lanes of an \xe2\x80\xa6EfShuffle 128 bits of integer data from <code>$a</code> and <code>$b</code> using an \xe2\x80\xa6EaShuffle the high <code>i16</code> lanes in <code>$a</code> using an immediate \xe2\x80\xa6EhShuffle the low <code>i16</code> lanes in <code>$a</code> using an immediate control \xe2\x80\xa6BgStore data from a register into memory.0C`Checks if the TypeId implements the trait of \xe2\x80\xa6DjReturns <code>self</code> with the checked field value replaced, if it \xe2\x80\xa6BjUnsets <code>TopLevelStorageAccess</code>.DfCreates an empty <code>HashMap</code> with the specified capacity, \xe2\x80\xa6DhCreates an empty <code>HashSet</code> with the specified capacity, usingCmConverts an address back to a mutable pointer, picking up \xe2\x80\xa6FeReturns <code>self</code> with <code>RESET_STYLE_ON_DROP</code> inserted if <code>condition</code>\xe2\x80\xa6DhReturns <code>self</code> with <code>RESET_STYLE_ON_DROP</code> removed.EbCasts <code>self</code> to <code>isize_down</code> wrapping at the numeric bounds.EeCasts from <code>i8</code> to <code>isize_down</code> wrapping at the numeric bounds.EbCasts from <code>i64</code> to <code>isize_down</code> wrapping at the numeric \xe2\x80\xa6EbCasts from <code>i16</code> to <code>isize_down</code> wrapping at the numeric \xe2\x80\xa6EeCasts from <code>u8</code> to <code>isize_down</code> wrapping at the numeric bounds.EbCasts from <code>u32</code> to <code>isize_down</code> wrapping at the numeric \xe2\x80\xa6EcCasts from <code>i128</code> to <code>isize_down</code> wrapping at the numeric \xe2\x80\xa6EbCasts from <code>i32</code> to <code>isize_down</code> wrapping at the numeric \xe2\x80\xa6EcCasts from <code>u128</code> to <code>isize_down</code> wrapping at the numeric \xe2\x80\xa6EdCasts from <code>usize</code> to <code>isize_down</code> wrapping at the numeric \xe2\x80\xa6EbCasts from <code>u64</code> to <code>isize_down</code> wrapping at the numeric \xe2\x80\xa6EdCasts from <code>isize</code> to <code>isize_down</code> wrapping at the numeric \xe2\x80\xa6EbCasts from <code>u16</code> to <code>isize_down</code> wrapping at the numeric \xe2\x80\xa6<<EbCasts <code>self</code> to <code>usize_down</code> wrapping at the numeric bounds.EdCasts from <code>isize</code> to <code>usize_down</code> wrapping at the numeric \xe2\x80\xa6EeCasts from <code>u8</code> to <code>usize_down</code> wrapping at the numeric bounds.EbCasts from <code>i64</code> to <code>usize_down</code> wrapping at the numeric \xe2\x80\xa6EcCasts from <code>i128</code> to <code>usize_down</code> wrapping at the numeric \xe2\x80\xa6EeCasts from <code>i8</code> to <code>usize_down</code> wrapping at the numeric bounds.EbCasts from <code>u64</code> to <code>usize_down</code> wrapping at the numeric \xe2\x80\xa6EbCasts from <code>u16</code> to <code>usize_down</code> wrapping at the numeric \xe2\x80\xa6EbCasts from <code>i32</code> to <code>usize_down</code> wrapping at the numeric \xe2\x80\xa6EcCasts from <code>u128</code> to <code>usize_down</code> wrapping at the numeric \xe2\x80\xa6EdCasts from <code>usize</code> to <code>usize_down</code> wrapping at the numeric \xe2\x80\xa6EbCasts from <code>i16</code> to <code>usize_down</code> wrapping at the numeric \xe2\x80\xa6EbCasts from <code>u32</code> to <code>usize_down</code> wrapping at the numeric \xe2\x80\xa6<<CfWrites 0..=10 decimal digits without leading zeros \xe2\x80\xa6CfWrites 0..=39 decimal digits without leading zeros \xe2\x80\xa6CfWrites 0..=20 decimal digits without leading zeros \xe2\x80\xa6CnWrites 0..=3 decimal digits without leading zeros starting \xe2\x80\xa6CnWrites 0..=5 decimal digits without leading zeros starting \xe2\x80\xa62Dk\xe2\x9c\xa8 \xf0\x9f\xab\x86 An example span handle into <code>ArenaBytesAllocExample</code>\xe2\x80\xa600Dk\xf0\x9f\x90\x9b \xf0\x9f\xaa\xb5 \xf0\x9f\x96\xbc\xef\xb8\x8f \xf0\x9f\xae\x96 Static debug logger for <code>SixelEncoder</code>\xe2\x80\xa6000DaCounts the number of 1s in <code>self</code> from the checked \xe2\x80\xa600BoConverts CIE lightness (L*) to linear luminance0BoConverts linear luminance to CIE lightness (L*)0EaReturns <code>true</code> if all bits in <code>RESET_STYLE_ON_DROP</code> are \xe2\x80\xa6DkDecodes a basic quoted string payload into <code>out</code> and returns \xe2\x80\xa6DjWrites the unique prime factors in the given <code>fbuffer</code>, and \xe2\x80\xa600000000000DgFills <code>out</code> with layout symbols derived from the Unicode \xe2\x80\xa6DhFinds the index of the first 1 in <code>self</code> from the checked \xe2\x80\xa600000DgFinds the index of the last 0 in <code>self</code> from the checked \xe2\x80\xa600000DhConverts bytes to an <code>OsString</code> without checking that the \xe2\x80\xa6CkConverts a slice of bytes to an OS string slice without \xe2\x80\xa6CjWrites the prime factors in the given <code>buffer</code>.000BlCalls `NumInt::int_ref_factors_prime_unique.0ChSimilar to int_factors_prime_unique, but operates on \xe2\x80\xa601010ClReturns <code>true</code> if the set shares any bit with \xe2\x80\xa6EgConsumes the <code>Box</code>, returning a wrapped <code>NonNull</code> pointer and \xe2\x80\xa6CjReturns an iterator over generated IDs using the given \xe2\x80\xa6EeMultiply <code>i16</code> lanes producing <code>i32</code> values, horizontal add \xe2\x80\xa60DeReturns an inclusive subslice up to the given <code>end</code> index.BoReturns the stored bytes occupied by one pixel.00DeSearch for <code>needle</code> in `haystack, with explicit string \xe2\x80\xa6DeSearch for <code>needle</code> in `haystack, with implicit string \xe2\x80\xa6DjSets <code>TopLevelStorageAccess</code> if <code>condition</code> is true.EaShuffle the high <code>i16</code> lanes in <code>$a</code> using an immediate \xe2\x80\xa6EhShuffle the low <code>i16</code> lanes in <code>$a</code> using an immediate control \xe2\x80\xa6DfReturns the exclusive slice omitting the last <code>n</code> elements.Dc\xe2\x9c\xa8 \xf0\x9f\xab\x86 An index handle into <code>ArenaStringAllocExample</code>.00Cn\xf0\x9f\x95\x98 \xf0\x9f\x9a\xa9 <code>core</code> Error returned from converting \xe2\x80\xa600Bo<code>203 Non-Authoritative Information</code>.Bo<code>407 Proxy Authentication Required</code>.Bo<code>451 Unavailable For Legal Reasons</code>.DaCounts the number of 0s in <code>self</code> from the checked \xe2\x80\xa600DhWrites the prime factors in <code>pfbuf</code>, and the unique prime \xe2\x80\xa600DjWrites the prime factors in <code>pfbuf</code>, and the unique factors \xe2\x80\xa600100110DhFinds the index of the first 0 in <code>self</code> from the checked \xe2\x80\xa600000CfUnsafely creates a C string wrapper from a byte slice.BgCreates a layout, bypassing all checks.DcDecomposes a <code>Vec&lt;T&gt;</code> into its raw components: \xe2\x80\xa6DnReturns a subslice from <code>start</code> (inclusive) to <code>end</code> \xe2\x80\xa6EbCasts <code>self</code> to <code>isize_down</code> clamping at the numeric bounds.EdCasts from <code>isize</code> to <code>isize_down</code> clamping at the numeric \xe2\x80\xa6EbCasts from <code>u32</code> to <code>isize_down</code> clamping at the numeric \xe2\x80\xa6EbCasts from <code>i16</code> to <code>isize_down</code> clamping at the numeric \xe2\x80\xa6EcCasts from <code>u128</code> to <code>isize_down</code> clamping at the numeric \xe2\x80\xa6EeCasts from <code>i8</code> to <code>isize_down</code> clamping at the numeric bounds.EbCasts from <code>i32</code> to <code>isize_down</code> clamping at the numeric \xe2\x80\xa6EdCasts from <code>usize</code> to <code>isize_down</code> clamping at the numeric \xe2\x80\xa6EbCasts from <code>i64</code> to <code>isize_down</code> clamping at the numeric \xe2\x80\xa6EbCasts from <code>u64</code> to <code>isize_down</code> clamping at the numeric \xe2\x80\xa6EeCasts from <code>u8</code> to <code>isize_down</code> clamping at the numeric bounds.EcCasts from <code>i128</code> to <code>isize_down</code> clamping at the numeric \xe2\x80\xa6EbCasts from <code>u16</code> to <code>isize_down</code> clamping at the numeric \xe2\x80\xa6<<EbCasts <code>self</code> to <code>usize_down</code> clamping at the numeric bounds.EbCasts from <code>i64</code> to <code>usize_down</code> clamping at the numeric \xe2\x80\xa6EbCasts from <code>u16</code> to <code>usize_down</code> clamping at the numeric \xe2\x80\xa6EcCasts from <code>u128</code> to <code>usize_down</code> clamping at the numeric \xe2\x80\xa6EdCasts from <code>isize</code> to <code>usize_down</code> clamping at the numeric \xe2\x80\xa6EbCasts from <code>i32</code> to <code>usize_down</code> clamping at the numeric \xe2\x80\xa6EbCasts from <code>u32</code> to <code>usize_down</code> clamping at the numeric \xe2\x80\xa6EbCasts from <code>i16</code> to <code>usize_down</code> clamping at the numeric \xe2\x80\xa6EeCasts from <code>i8</code> to <code>usize_down</code> clamping at the numeric bounds.EeCasts from <code>u8</code> to <code>usize_down</code> clamping at the numeric bounds.EcCasts from <code>i128</code> to <code>usize_down</code> clamping at the numeric \xe2\x80\xa6EdCasts from <code>usize</code> to <code>usize_down</code> clamping at the numeric \xe2\x80\xa6EbCasts from <code>u64</code> to <code>usize_down</code> clamping at the numeric \xe2\x80\xa6<<DeSearch for <code>needle</code> in `haystack, with explicit string \xe2\x80\xa6DeSearch for <code>needle</code> in `haystack, with implicit string \xe2\x80\xa6CnReplaces the byte-frame storage with an initialized vector \xe2\x80\xa6C`WebAssembly animation-frame callback dispatcher.EaReturns <code>self</code> with <code>TopLevelStorageAccess</code> inserted if \xe2\x80\xa6DjReturns <code>self</code> with <code>TopLevelStorageAccess</code> removed.Bj\xe2\x9c\xa8 \xf0\x9f\xab\x86 A canonical string symbol for \xe2\x80\xa600Cj\xf0\x9f\x94\x84 \xf0\x9f\x8d\x83 A lending iterator that can inspect the next \xe2\x80\xa600EcReturns <code>true</code> if all bits in <code>TopLevelStorageAccess</code> are \xe2\x80\xa6EgConverts the lower <code>f32</code> to <code>f64</code> and replace the low lane of \xe2\x80\xa6EfConverts the low <code>f64</code> to <code>f32</code> and replaces the low lane of \xe2\x80\xa6ClRounds the <code>f32</code> lanes to <code>i32</code> lanes.ClConvert <code>f32</code> lanes to be <code>i32</code> lanes.DfRounds the four <code>i32</code> lanes to four <code>f32</code> lanes.ClConvert <code>i32</code> lanes to be <code>f32</code> lanes.DjReturns <code>Some(numeric_value)</code> (0-9) of the decimal digit at \xe2\x80\xa600000DjReturns <code>Some(numeric_value)</code> (0-15) if the index is within \xe2\x80\xa600000DfCreates a <code>charu</code> from an array of UTF-8 bytes, without \xe2\x80\xa6DdCreates a <code>charu_niche</code> from an array of UTF-8 bytes, \xe2\x80\xa6CkSlowly inserts an <code>m128i</code> to <code>m256i</code>.ClReturns <code>true</code> if the set shares any bit with \xe2\x80\xa6CiReturns an exclusive subslice up to and including the \xe2\x80\xa6CjReturns an exclusive substring up to and including the \xe2\x80\xa6DdSets the given checked <code>value</code> into the bits from the \xe2\x80\xa600000CmWrites the sixel color definition bytes for a given color \xe2\x80\xa6ClWrites a string slice, returning the number of the bytes \xe2\x80\xa6Ca<code>511 Network Authentication Required</code>.Ca<code>431 Request Header Fields Too Large</code>.DgFinds the index of the last 1 in <code>self</code> from the checked \xe2\x80\xa600DlRounds the two <code>f64</code> lanes to the low two <code>i32</code> lanes.ClConvert <code>f64</code> lanes to be <code>i32</code> lanes.ClConvert <code>i32</code> lanes to be <code>f64</code> lanes.CjReturns the glyph for the specified Unicode scalar, if \xe2\x80\xa6DhFinds the index of the first 1 in <code>self</code> from the checked \xe2\x80\xa600DgFinds the index of the last 0 in <code>self</code> from the checked \xe2\x80\xa600EeGets the <code>f32</code> lane requested. Returns as an <code>i32</code> bit pattern.DjAttempts to get mutable references to <code>N</code> values in the map \xe2\x80\xa6DgReturns whether <code>data_len</code> contains complete interleaved \xe2\x80\xa6C`Calls `NumInt::int_ref_factors_prime_unique_buf.0ClSimilar to int_factors_prime_unique_buf, but operates on \xe2\x80\xa601010ClReturns <code>true</code> if the set shares any bit with \xe2\x80\xa6DeReturns an inclusive subslice up to the given <code>end</code> index.CkReturns if, among the masked bits, there\xe2\x80\x99s both 0s and 1sDhFinds the index of the first 0 in <code>self</code> from the checked \xe2\x80\xa600DlRounds the two <code>f64</code> lanes to the low two <code>f32</code> lanes.DdSets the given checked <code>value</code> into the bits from the \xe2\x80\xa600CjConvert <code>i8</code> values to <code>i16</code> values.CjConvert <code>u8</code> values to <code>i16</code> values.DkLike <code>fill_symbols_from_chars_with</code>, but the mapping closure \xe2\x80\xa6DkComputes eight <code>u16</code> \xe2\x80\x9csum of absolute difference\xe2\x80\x9d values \xe2\x80\xa60DiAdd horizontal pairs of <code>i16</code> values, saturating, pack the \xe2\x80\xa6DhHorizontal saturating <code>a + b</code> with lanes as <code>i16</code>.CkConvert <code>i16</code> values to <code>i32</code> values.CkConvert <code>u16</code> values to <code>i32</code> values.CkConvert <code>i32</code> values to <code>i64</code> values.CkConvert <code>u32</code> values to <code>i64</code> values.DjSubtract horizontal pairs of <code>i16</code> values, saturating, pack \xe2\x80\xa6DhHorizontal saturating <code>a - b</code> with lanes as <code>i16</code>.DjAttempts to get mutable references to <code>N</code> values in the map \xe2\x80\xa6DjRounds the lower two <code>i32</code> lanes to two <code>f64</code> lanes.DiConvert <code>f32</code> lanes to <code>i32</code> lanes with truncation.DiConvert <code>f64</code> lanes to <code>i32</code> lanes with truncation.AgThis is dumb and weird.0DnConvert the lower eight <code>i8</code> lanes to eight <code>i16</code> lanes.DbConvert lower 4 <code>u8</code> values to <code>i16</code> values.DbConvert lower 8 <code>u8</code> values to <code>i16</code> values.DlConvert the lower four <code>i8</code> lanes to four <code>i32</code> lanes.DfConvert the lower 8 <code>i8</code> values to <code>i32</code> values.DjConvert the lower two <code>i8</code> lanes to two <code>i64</code> lanes.DfConvert the lower 4 <code>i8</code> values to <code>i64</code> values.DnConvert the lower eight <code>u8</code> lanes to eight <code>u16</code> lanes.DlConvert the lower four <code>u8</code> lanes to four <code>u32</code> lanes.DjConvert the lower two <code>u8</code> lanes to two <code>u64</code> lanes.DkConvert the lower two <code>i64</code> lanes to two <code>i32</code> lanes.DmConvert the lower four <code>i16</code> lanes to four <code>i32</code> lanes.DkConvert the lower two <code>i32</code> lanes to two <code>i64</code> lanes.CkConvert <code>i16</code> values to <code>i64</code> values.CkConvert <code>u16</code> values to <code>i64</code> values.DmConvert the lower four <code>u16</code> lanes to four <code>u32</code> lanes.DkConvert the lower two <code>u16</code> lanes to two <code>u64</code> lanes.DkConvert the lower two <code>u32</code> lanes to two <code>u64</code> lanes.")